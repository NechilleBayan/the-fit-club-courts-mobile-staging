# Decision log: Admin console integration

Append-only. Newest last. One row per decision, and a decision is anything that
removed, merged, renamed, reassigned, or overrode something that existed before.

The test this log has to pass: Someone reading only this file can find anything
that disappeared, and can tell a deliberate call from an accident.

Where the prompt was silent and judgement was exercised, the row says so and
names the alternative that was rejected.

| # | Phase | Decision | What changed | Why | Reversible? |
|---|---|---|---|---|---|
| 1 | 0 | Baseline is commit `f46784e`, not `60399fb` | Branch `admin-console-integration` cut from `main` at `60399fb`. The `admin/` folder was untracked on `main`, so it was committed verbatim as `f46784e` before any edits. | A 27-file folder that git has never seen produces no diff. Every later change to `admin/` would have arrived as an undifferentiated block of new files, which makes the branch unreviewable. `f46784e` contains zero edits, so it is a true baseline. | Yes. `git diff f46784e -- admin/` is the whole of this branch's admin work. |
| 2 | 0 | Visual baselines are gitignored, not committed | Added `.baseline/` to `.gitignore`. 104 PNGs live on disk at `.baseline/{mobile,desktop}/` for the run. | The prompt allowed either. 104 binaries, regenerated at every checkpoint, would dominate the history of a branch whose readable diff is the deliverable. Rejected alternative: Commit them and accept the noise. | Yes. Delete the ignore line and `git add .baseline`. |
| 3 | 0 | Screenshot runner lives outside the repo | `shoot.mjs` plus its `playwright` install sit in the session scratchpad, not in the working tree. | The prototype's stated identity is no npm, no build step, no dependencies. Putting a `package.json` and a `node_modules` at the repo root to verify that claim would falsify it. The runner is verification tooling for this branch, not part of the prototype. Rejected alternative: A `tools/` folder in the repo with its own `package.json`. | Yes, and the script is reproduced in `ADMIN-INTEGRATION.md` so the run can be repeated. |
| 4 | 0 | Unreferenced carousel images left untracked | `carousel/c19.jpg` through `c37.jpg` remain untracked and uncommitted. | Nothing in `index.html`, `admin/`, or `preview.html` references them. They are out of this branch's scope and committing them would be an unrelated change riding along. Rejected alternative: Commit them for tidiness. | Yes. They are still on disk. |
| 5 | 0 | Prompt fact corrected: The staff shell does have desktop navigation | No code change. Recorded so Phase 2 is not built on a wrong premise. | The prompt states the staff shell has no desktop navigation above 1024 and that a Front Desk user gets the customer header as their only nav. Neither is true. `render()` populates `#bar-nav` and `#tabhost` from the same `STAFF_TABS` array on the staff branch, so the header carries Today, Scan, Walk-in, Open Play, and Exceptions with `aria-current` on the live one, and the 1280 baseline of `/staff/today` shows it. The comment above that branch is an explicit prior decision: "Staff keep theirs ... It is a different application wearing the same chrome." The Phase 2 sidebar therefore replaces a working, deliberately chosen desktop nav rather than filling a hole, which is a stronger claim than the prompt assumed and is settled in row 6. | n/a |
| 6 | 1 | The persisted key is `tfc-staging`, holding exactly six scalars | `{ surface, scenario, connectivity, capacity, clockOffset, theme }`, one JSON object, validated on every read. `S.orders` is not in it and never will be. | An order graph carries absolute hold deadlines, generated identifiers, and a share token per pass, and about thirty functions in `index.html` mutate it. Moving it across a document boundary means freezing all of that into a wire format and keeping two renderers honest about it forever. The console gets a scenario key and draws its own sample instead. | Yes. One file, one key. |
| 7 | 1 | Theme defaults to `null`, not to a colour | `getTheme()` returns `null` until somebody chooses. Each build passes its own fallback to `applyTheme()`: The customer build passes `dark`, the console passes `system`. | Picking a single global default would have repainted one of the two builds for a reader who never asked. The customer build ships `<html data-theme="dark">`; the console ships System selected and renders light. Either choice regresses 27 or 25 baseline screenshots on day one. `null` means "nobody has chosen, keep your own default", so both baselines are preserved and an explicit choice still crosses. Rejected alternative: Default everything to dark. | Yes. |
| 8 | 1 | The old `tfc-theme` key is migrated and then deleted | First read adopts a `dark`/`light` value from `tfc-theme`, writes it into `tfc-staging`, and removes the old key. | Keeping both would be two sources of truth for one setting, which is the exact thing this phase exists to remove. Migrating preserves the reader's current preference, which is the other half of the requirement. Rejected alternative: Keep mirroring writes to `tfc-theme` for backward compatibility. Note the consequence: `claude-version-index.html`, the superseded snapshot, still reads `tfc-theme` and will fall back to its own pinned default. It is a frozen snapshot and out of scope. | Yes, but the old key is gone once a browser has loaded the new build. |
| 9 | 1 | Behaviour change: Reset all state now clears the theme | It previously did not. The comment in `index.html` called the theme "the one thing in this build that outlives a refresh". | Checkpoint 1 asks that Reset return both surfaces to default, and a reset that leaves the console painted in a choice made twenty minutes ago in the other build is not a reset. The old comment has been rewritten to say the new rule rather than left to contradict the code. | Yes. Exclude `theme` from `STAGING.reset()`. |
| 10 | 1 | The armed scenario is remembered but not replayed in the customer build | `loadScenario()` writes the key. Boot restores surface, connectivity, capacity, and clock offset, but does not re-run the seeding. The harness marks the armed scenario with `aria-pressed` so it is still visible. | Replaying on load would mint a fresh order with a new identifier and a new hold deadline while the address bar still pointed at the order the previous run created, which is a 404 dressed as a feature. Dials persist; bookings do not. Rejected alternative: Re-seed on boot and rewrite the hash. | Yes. |
| 11 | 1 | Deviation from the prompt: The bridge is a `<script>` tag in each admin page's head, not a dynamic load from `admin.js` | 27 admin pages gained `<script src="../staging-state.js" data-default-theme="system">` in the head, ahead of the stylesheet. `admin.js` subscribes but does not load the file. | The prompt said to load it from `admin.js`. `admin.js` is at the foot of the body, so anything it loads runs after the first paint, and the first thing this file does is settle the theme. Loading it there is a guaranteed flash of the wrong theme on every one of 27 pages. The default is declared on the tag itself so each document names its own fallback in one place. | Yes. |
| 12 | 1 | `index.html` is no longer a single self-contained document | It now loads one external script, `staging-state.js`, from its own origin. | It already loaded 18 carousel JPEGs and ten font files, so this is not the first request, but it is the first script. The alternative was pasting the same 250 lines into both builds, which is two sources of truth for the thing whose whole purpose is to be one. Consequence: Opening `index.html` over `file://` now depends on the browser allowing a sibling script and an origin-null localStorage. The project already specifies a local server. | Yes. |
| 13 | 1 | `ACTIONS.resetall` split into `resetRuntime()` plus `STAGING.reset()` | `loadScenario()` now calls `resetRuntime()` rather than `ACTIONS.resetall()`. | Loading a scenario has to start from a clean runtime but must not throw away the reader's theme, which now lives in the persisted half. Only the button that says it resets everything clears the key. Side effect: `loadScenario()` no longer fires an intermediate `go("/")`. That was invisible, because `go()` sets `location.hash` and every scenario navigates again before the resulting `hashchange` fires, so only the final route was ever rendered. | Yes. |
| 14 | 1 | `subscribe()` callbacks receive a third argument, `origin` | `"local"` for a change made in this document, `"remote"` for one that arrived by storage event. | Without it, a control that has already updated its own runtime and rendered gets told to do it again on its own echo, so every tap renders twice. The prompt specified a change-subscription hook and left the shape open. Also added: Setters for connectivity, capacity, and clock offset, which the prompt listed only as getters, because the harness has to write them. | Yes. |
| 15 | 1 | Copy rewritten in three places, because it had become untrue | `openDemo()`'s intro, `scrDemo()`'s lede plus a new "What survives a refresh" section, and `admin/settings.html`'s "nothing is saved" sandbox note. | All three promised that nothing persisted. The theme picker in Settings, in particular, sat under a note saying nothing is saved while being the one control that now genuinely saves. The sandbox-honesty rule makes fixing this mandatory rather than optional. | Yes. |
| 16 | 1 | Verification harness: Pixel diff with a stated noise floor, not byte equality | Screenshots freeze the clock, force every declared font face to load, decode every image, and disable animation and transition before capture. Comparison is `pixelmatch`, reporting differing pixel counts. | Byte equality produced 11 false positives out of 104 against an unchanged tree. The largest cause was the 18 grayscale film-strip frames carrying `decoding="async"`: An image can report `complete` and still be undecoded at capture. The second was the split latin / latin-ext fonts, where the peso sign at U+20B1 starts a second font fetch only after `document.fonts.ready` has already resolved, worth 22 pixels of reflow. Residual noise floor after all of that: `desktop/app__court` and `desktop/app__event` still flip height occasionally ON THE BASELINE TREE, by 1px and 23px. Both were confirmed to flake on `f46784e` with no changes applied, so neither is attributable to this branch. | n/a |
| 17 | 2 | The collision is resolved in the sidebar's favour: The staff shell gives up its header nav | At 1024px and up, `body.has-sidenav .topnav{display:none}`. Staff destinations move from the two strip header into the rail. The customer path is untouched at every width. | Row 5 established that the staff header nav already worked and was deliberate. It is replaced anyway, because the split that matters is product, not width. A visitor reads down a page toward one conversion action; a Front Desk staffer moves between five destinations with no hero and nothing to convert. The second is a console, and it is the same console `admin/` is, so it wears the same rail. The prior comment's own premise, that staff are "a different application wearing the same chrome", is what argues for this: Staff being a different application was right, and the conclusion has moved from same chrome to its own. Rejected alternative: Leave the header nav alone and give the rail only to `admin/`. Cheaper, regresses nothing, and turned down because it leaves two staff-facing desktop chromes in one product. Customer differing from staff is a distinction a reader learns once. Front Desk differing from Ops Manager is one nobody can. | Yes. Delete one CSS rule and one `paintSidenav` call. |
| 18 | 2 | `TABS` and `DRAWER` moved out of `admin.js` into a new `console-nav.js` | `admin.js` now reads `window.CONSOLE_NAV`, keeping the old local names so the rest of the file reads as it did. `DRAWER` is derived by flattening the grouped model, which is the single sequence the dialog has always rendered. | Two documents now draw the same destination list. A second copy is a second place to add a screen, and therefore a place to forget to. Also moved: The sidebar renderer itself, so the two builds cannot drift on the current-page marker either. | Yes. |
| 19 | 2 | The sidebar's stylesheet is shared too, in `console-nav.css`; the layout around it is not | Component rules live in `console-nav.css`, loaded by both builds. Shell layout stays per document: The grid in `admin.css`, the fixed offset in `index.html`. | The rail is the same rail on both sides, so drawing it twice risks the same divergence a duplicated destination list does. The shells are genuinely not alike, so their layouts stay separate. The shared sheet names no colours, only tokens, which both builds declare; that is what lets one file serve two palettes that are inverses of each other. `.countpill` is redeclared inside it because `admin.css` had it globally and the customer build had it nowhere, so the component would have rendered unstyled on one side. | Yes. |
| 20 | 2 | The customer build uses a fixed rail with a padding offset; the console uses a grid column | `body.has-sidenav #shell{padding-inline-start:var(--sidenav-w)}` plus `position:fixed` on the rail, versus `#shell{display:grid}` in the console. The rail element is also inserted as a sibling of `#shell` rather than a child. | The console owns a plain `#shell` it can turn into two columns. The customer `#shell` is a flex column carrying a sticky header group, a full bleed hero container, and a fixed dock, and re-parenting all of that into grid tracks to move a navigation is a far larger change for no more result. It also carries `overflow-x:clip`, and a fixed element inside a clipping ancestor is a bet on which browser is reading the spec that week, hence the sibling placement. Rejected alternative: Force the grid on both for symmetry. | Yes. |
| 21 | 2 | The rail follows the path, not the shell: `body.has-sidenav`, not `body.shell-staff` | `paintSidenav` shows the rail when `view.shell === "staff"` OR the path starts with `/staff/`. | The prompt listed five staff routes. Two of them, `/staff/walkin` and `/staff/openplay/:id`, return the TASK shell, not the staff shell, and so have never had the mobile tab bar either. Keying on the shell would have left them railless and jumped the whole page 264px sideways every time a staffer started or finished a walk-in. The reason a phone withholds navigation on a task screen is vertical budget, and that constraint does not exist at 1280px. So the two renderings of one nav model legitimately disagree about task screens. `/staff/booking/:id` is included by the same rule. Rejected alternative: Match the mobile rule exactly and accept the jump. | Yes. |
| 22 | 2 | The persisted shape gained a seventh field, `navCollapsed` | `{ surface, scenario, connectivity, capacity, clockOffset, theme, navCollapsed }`. | Phase 1 documented the shape as exactly six scalars, and this breaks that. The prompt requires the collapse choice to persist via `STAGING`, and the field is the same category as `theme`: A preference about chrome rather than a harness dial. The file header now says six dials plus one chrome preference rather than pretending it is seven of a kind. | Yes. |
| 23 | 2 | Front Desk does not see the Money group, and the rail says so | `CONSOLE_NAV.groupsFor(surface)` returns the money group only for `opsmanager`. The staff rail carries a note naming the absence. | The permission boundary between Ops Manager and Front Desk is the load-bearing distinction in this product and the exception queue already draws it. A rail that silently omits three rows teaches nothing; one that says the money area is the Operations Manager's, and that the server denies it too, demonstrates the boundary. The test is an ALLOW, not a deny: The first version listed the surfaces refused, which admitted the harness's own `visitor` by default and showed revenue reports to nobody in particular on a cold start. | Yes. |
| 24 | 2 | The Session group is dropped from the staff rail; the console keeps it | `paintSidenav` filters out `Session`. | There is no session in the customer build to end. The surface is a harness dial, not a login, so a Log Out row there would be the first control in the build that does nothing, which the sandbox-honesty rule forbids. The console has an identity to sign out and keeps its row. | Yes. |
| 25 | 2 | Seven glyphs added to the customer build's sprite, at that sprite's stroke weight | `building`, `key`, `card`, `chart`, `bell`, `activity`, `settings` added as `<symbol>`s in `index.html`, path data lifted from `admin.js`. A `NAV_GLYPH` table maps the model's names onto this sprite's existing ids (`calendar` to `cal`, `user` to `person`, `hours` to `clock`, `help` to `info`). | The shared model names glyphs in the console's vocabulary and this sprite was missing seven of them. Same path data, so the two builds draw the same bell rather than two bells. Stroke weight is this file's 2 rather than the console's 1.75, because a symbol wearing the console's weight would be the one icon in the document drawn lighter than its neighbours. That the two sprites disagree on weight AND on mechanism (`<symbol>` plus `<use>` versus path strings) is noted here for Phase 3c to settle; it did not need settling to render a navigation. | Yes. |
| 26 | 2 | The pending-tasks count uses the quiet pill, not the red one | `quiet:true` on the More row; Notifications keeps `--critical`. | The console's own token comment reserves its only red for the unread badge and for states that cost money or close a court. Five tasks sitting in a queue is neither. The collapsed rail follows the same rule: A quiet count becomes a grey dot, not a red one. | Yes. |
| 27 | 2 | The rail identity shows role only, not role plus club | `head.sub` is `DATA.admin.role` in the console, `VENUE.name` in the staff shell. | The drawer can afford both because its panel is 19rem of a phone screen with nothing beside it. The rail is narrower and the club name is the half that truncates, leaving "Club Manager, The F..." saying less than "Club Manager" does. | Yes. |
| 28 | 2 | Collapsed labels are clipped, not removed | `opacity:0; width:0; flex:0 0 0` rather than `display:none`. | `display:none` removes the text from the accessibility tree along with the layout, and every row in the icon rail would then announce as a link with no name. Clipped, the label is still the row's accessible name. Rejected alternative: An `aria-label` on every row, which duplicates the visible text when the rail is expanded. | Yes. |
| 29 | 2 | The drawer is closed when a narrow window is widened past 1024 | A `matchMedia` listener in `admin.js` closes `#drawer` on the way up. | The drawer and the rail are the same navigation at two widths and must never both be present. Neither stylesheet can close a dialog that is already open, so a reader who opens the drawer at 900px and drags the window to 1200px would otherwise be looking at a modal sitting over a navigation that says the same thing. | Yes. |
| 30 | 3a | The roster becomes a real array in `admin.js`, and `pendingTasks` is derived from it | `DATA.staff` holds the two people with their per-status task counts, `DATA.unassigned` holds the unowned work, and `DATA.pendingTasks` and `DATA.onDuty` are computed rather than typed. `DATA.admin` is now `STAFF[0]` rather than a separate literal. | The file's stated premise is that the badge, the drawer count, and the dashboard figure cannot disagree. A hand-typed `pendingTasks: 5` beside a hand-edited queue can, and after moving five tasks between three owners it very nearly did. Deriving it makes the disagreement impossible rather than merely unlikely. | Yes. |
| 31 | 3a | Removed: Paolo Reyes, Maintenance, 18 references across 12 files | `activity.html` (completed lighting check) to Rea. `bookings.html`, `courts.html`, `court-details.html` x2, `dashboard.html`, `maintenance.html` x2, `notifications.html` to **Unassigned** where the reference was live work. `court-details.html` and `maintenance.html` timeline entries (3) to Rea, because a log records what happened and "Unassigned did this" is a lie about the past. `staff.html` and `staff-manage.html` rows deleted. `tasks.html`: The assignee option deleted, two open tasks to Unassigned, one completed task to Rea. | Maintenance is the role with nobody in it, so its live work is genuinely unowned and Unassigned is the honest state rather than a gap. The split between live work and log entries is the rule applied throughout: A future or in-flight job can have no owner, a completed one cannot. | Yes. |
| 32 | 3a | Removed: Carlo Santos, Coach, 6 references across 5 files | `open-play.html` Coach field to **Unassigned**. `check-in.html` Beginner Clinic host to Rea. `tasks.html`: The assignee option deleted, and "Confirm Sunday coaching slots" reassigned to Rea. `staff.html` and `staff-manage.html` rows deleted. | The club has no coach on staff, so the coach slot on a session is empty and says so. The clinic still has to be run by somebody today, and confirming outside coaching is a manager's job, so those two went to Rea rather than into the void. | Yes. |
| 33 | 3a | Removed: Mika Dizon, Front Desk, 4 references across 4 files | `staff-details.html` sentence rewritten (see row 36). `tasks.html` assignee option deleted. `staff.html` and `staff-manage.html` rows deleted. | Mika held nothing: The row said "No tasks" and the only prose reference existed to point at that emptiness. Nothing needed reassigning. | Yes. |
| 34 | 3a | Removed: Bea Lorenzo, Front Desk, 4 references across 2 files | `staff.html` row deleted, and the Shift Unfilled plate rewritten (row 37). `tasks.html`: The assignee option deleted, and "Wipe down seating area" reassigned to **Unassigned, Evening desk shift**. | Bea was the evening desk cover. With her gone the evening shift is genuinely unfilled, which the console already had a plate for and which is better demo material than a name. Her one task follows the shift rather than a person. | Yes. |
| 35 | 3a | Removed: Aldrin Torres, Coach, and Nico Garcia, Maintenance, 1 reference each, both in `staff.html` | Both rows deleted outright. | Neither appeared anywhere else in 27 files. They were list padding: An off-duty coach and a maintenance staffer on leave, existing to make a roster look longer. Nothing to reassign and nothing lost. | Yes. |
| 36 | 3a | `staff-details.html` stopped pointing at somebody with spare capacity | "Consider giving the next job to Mika Dizon, who has none" became "There is nobody to hand the next job to: Rea is carrying two of her own and the evening desk shift is unfilled." | The sentence existed to show a manager the obvious next move. With a cast of two there is no obvious next move, and saying so is the more useful screen: The console's job here is to show that the club is at capacity, not to invent a name with slack. | Yes. |
| 37 | 3a | The Shift Unfilled plate now says nobody, not a name | "Bea Lorenzo covers to 10:00 PM but is alone on the desk" became "Unassigned. Both staff finish by 3:00 PM, and Evening Open Play runs 7:00 PM to 11:00 PM across all four courts." | The plate was already flagging a cover problem and now flags a starker one, which is the correct reading of a two person roster that ends at 3:00 PM against a session that runs to 11:00 PM. It also ties the console's staffing to the customer build's Open Play window times for the first time. | Yes. |
| 38 | 3a | Counts updated wherever the cull moved them | `staff.html`: On Duty Now 5 of 8 to 2 of 2; "8 Staff Members" to "2 Staff Members"; Joy's chip 3 tasks to 2. `staff-manage.html`: "8 Accounts" to "2 Accounts"; role tallies 2/3/3 to 1/1/Nobody; "3 more accounts" line deleted. `dashboard.html`: Staff On Duty 5 to 2; "waiting on 3 staff members" to "Three have an owner, two do not." `tasks.html`: Lede and assignee options. `staff-details.html`: Joy's "3 open tasks" to 2. | These are the figures the prompt named. `unread` (4) and the 9 completed tasks are unchanged, because neither depends on how many people are on the roster. | Yes. |
| 39 | 3a | Pre-existing bug fixed: Overdue was a fourth bucket that nothing counted | `tasks.html` lost one Pending row ("Print weekly rate card for the desk", Joy), and the queue comment now states that Overdue is a late Pending rather than a stage of its own. | Found while reconciling counts, not introduced by it. The queue showed 5 rows tagged Pending PLUS 1 tagged Overdue, while `DATA.pendingTasks`, the dashboard, and the staff sumcard all said 5 pending with 1 overdue among them. The segmented control's "All 16" equalled 5 + 2 + 9, which excluded the overdue row entirely, so the console disagreed with itself by one in whichever direction a reader added it up. Now: Pending is 5 with 1 late, Active 2, Done 9, All 16. The deleted task was also removed from Joy's detail page, where it was listed a second time. | Yes, but it would restore an inconsistency. |
| 40 | 3a | The Off Duty filter was removed from `staff.html` | One `<button class="filter">` deleted, with a comment saying why. | Both remaining staff are on shift, so the filter selected an empty list every time it was pressed. The filters in this sandbox only paint, so it would not even have looked empty; it would have looked broken. The three that remain (All, On Duty, Has Pending Tasks) all describe a set that exists. | Yes. |
| 41 | 3a | Name collision resolved by the cull, and the player keeps the name | No change to `index.html`. `Mika` remains an Open Play player in three places: The `names` scenario seed, the Open Play roster sample, and "Mika R." in the Today arrivals list. | The collision was between a player called Mika and a Front Desk staffer called Mika Dizon. Mika Dizon is gone from all 27 console pages, verified by rendering each one and matching the text, so no screen shows the player while a staffer of that name exists. The arrivals entry is explicitly a customer arriving at the desk. Renaming the player as well would be a change to the customer build's reviewed sample data for a collision that no longer exists. | n/a |
| 42 | 3b | The contact sheet is gone; `admin/index.html` is now a redirect | The 26-row hand-maintained index was deleted. What replaces it is a three-line redirect to `dashboard.html`: `location.replace` first so the page never enters history and Back does not bounce, a `meta refresh` for scripting off, and a real sentence with a link for both off. | The filename has to keep existing, because a request for `admin/` with no document resolves to it and a static server would otherwise answer with a directory listing or a 404. Neither is a sensible landing. The dashboard is, which is what the prompt suggested and what this does. `replace` rather than `href` is the part worth noting: `href` would have put the redirect in the history and made Back unusable. | Yes, the old sheet is in `git show 01634ae:admin/index.html`. |
| 43 | 3b | All 26 console screens joined `ALL_ROUTES`, not a second array | Each entry is `[href, "adm-id, Label", group]`. The third element is the group heading and doubles as the flag that says href rather than hash. The renderer emits a heading when the group changes, drops the `#`, and drops `data-action="closesheet"` because the document is leaving anyway. | One array, because the whole reason the contact sheet had to go was that two hand-maintained indexes of one folder will disagree and the second one edited will be the wrong one. Putting the console in its own array would have rebuilt that problem inside one file. Ids follow the existing spirit: `adm-dashboard`, `adm-bookings`, and so on, 26 of them, all verified to resolve. | Yes. |
| 44 | 3b | The way back to the customer build lives in the System group | A `Customer Build` row was added to the shared `GROUPS`, marked `absolute` so the `admin/` prefix is not applied and `consoleOnly` so it never renders in the staff shell. It appears in the console's desktop rail and in its mobile drawer. | The old contact sheet held the only link back to `../index.html`, and deleting it would have made the console a folder you can enter and not leave except through the address bar. `consoleOnly` exists because the staff rail is already inside the customer build, where a link to the document you are reading is furniture. `groupsFor()` strips those rows for every caller, since its only caller is the customer build; the console reaches for `GROUPS` directly. | Yes. |
| 45 | 3c | Two builds described two different clubs. The customer build won | `club.js` is new and loaded by both. Address, phone, email, maps link and courts come from it. The console's Kapitolyo, Pasig address, its `+63 2 8555 0140` landline, and its `hello@thefitclubcourts.ph` address are gone from `admin.js`, `club-profile.html`, and `help.html`. `index.html`'s `VENUE` is now `window.CLUB` and `MAPS_URL` reads from it. `joy.m@thefitclubcourts.ph` became `joy.m@example.com`, matching the convention every other sample address in the build already used. | Not a wording difference: Silway-8, Polomolok, South Cotabato and Kapitolyo, Pasig City are 1,200 kilometres apart, in one prototype. The customer build wins because its address is load bearing, being what the Google Maps link resolves to and what a visitor is told to travel to, and because `thefitclubcourts.ph` was a domain nothing else in the project used. | Yes. |
| 46 | 3c | The hours argument went the other way, and the Monday closure was deleted | The club is open daily, 6:00 AM to 11:00 PM. `hours.html`'s seven rows are identical and say why. The weekly Monday closure is gone from `closures.html` (replaced by a "No recurring closures" empty state), from `reports.html` (Monday is now the quietest open day, bar and caption rewritten), from `club-profile.html`, and from `dashboard.html`. Two one-off Monday closures in `closures.html` and `court-details.html` kept their dates and were re-reasoned as a surface sweep. | The console had the more detailed story and it was the wrong one. It also contradicted the customer build's functional data: `OP_WINDOWS` sells Open Play from 7:00 PM to 11:00 PM, which is an hour past a 10:00 PM close. Hours the club is shut cannot be hours it is selling. Functional data beats the copy describing it, so the copy moved. Rejected alternative: Cut the Open Play window to 10:00 PM, which is a change to reviewed customer behaviour to preserve a sentence. Correction to an earlier draft of this row, recorded rather than edited away: It also claimed the customer build offers seven consecutive dates with no exclusions. It does not. `scrCourtWhen` has `closedDay = d.dayIndex === 3`, a closed day at a rolling three-day offset from today. That is sample data demonstrating the closed-day state, and because it rolls it can never line up with a fixed weekday, so it still cannot be the console's every-Monday closure. The Open Play argument above is the one that carries the decision. | Yes, but it would restore the contradiction. |
| 47 | 3c | Status vocabulary: `Reserved` became `Held`, and the console gained `Needs review` | Three booking rows in `dashboard.html`, `courts.html`, and `court-details.html`. The legend in `help.html` now reads Held rather than Reserved, adds Needs review to the red row, and carries a paragraph saying the words are the customer build's words. | `Reserved` and `Held` name one state, and they do not mean the same thing to a reader. The customer build spends real copy insisting this state is "Not booked yet" and that the time is released unless payment is verified; `Reserved` says the opposite in one word. `Needs review` was missing entirely, while the customer build's whole exception queue exists to close that loop and one of the eleven scenarios is called `mismatch`, so the console had no word for a state Phase 4 has to render. | Yes. |
| 48 | 3c | Deliberately left alone: `Paid` and `Unpaid` | Eleven and four uses, unchanged. | They look like they clash with `Confirmed` and `Awaiting payment` and they do not: they are a different axis. In this product a payment can arrive and NOT confirm an order, which is exactly what the amount-mismatch path is for, so paid and confirmed are genuinely separate facts about one booking and the console showing both is right. What IS a real divergence is underneath them: the console models bookings that sit unpaid for days ("PAYMENT OVERDUE, 3 days late") while the customer build releases an unpaid hold in about ten minutes. Reconciling that is a product decision about whether this club invoices, not a copy fix, and it is left open here on purpose so nobody has to rediscover the question. | n/a |
| 49 | 3c | Deliberately left alone: The two icon sprites are not merged | `index.html` keeps 57 `<symbol>` elements referenced by `<use>` at stroke weight 2. `admin.js` keeps 48 path strings injected inline at weight 1.75. | They are the same drawing language, which Phase 2 proved by lifting seven glyphs from one to the other unchanged, but they are not the same mechanism. Merging means either injecting an SVG sprite into 27 static documents and rewriting `svg()`, or converting 57 symbols to path strings and rewriting `icon()`. Both touch how every icon on 51 routes and 26 screens renders, both risk the mobile baselines this branch exists to protect, and neither changes a single pixel if done correctly. That is a large blast radius for zero visible gain. What was worth doing is done: where the two overlap, they now draw the same glyph. The stroke-weight difference (2 against 1.75) is real and visible if the two builds are put side by side; it is left because normalising it would repaint every icon in one of them. | n/a |
| 50 | 3c | Deliberately left alone: Customer initials and player names in the console | `AL`, `DO`, `GV`, `JB`, `KU`, `LR`, `MT`, `NC`, `RA` and their full names stay. | The cull was of the STAFF cast, which is the one that carries the permission boundary. Customers are the club's demand and a console with two of them would demonstrate less, not more. They also do not appear in the customer build, so there is no second copy to drift from. | n/a |
| 51 | 4a | `SCENARIOS` moved to a shared `scenarios.js` and became a model rather than a list of labels | Each entry now carries key, label, blurb, `surfaces`, a `seed`, and an `admin` block. `loadScenario()` reads the seed through four small helpers instead of a thirty-line switch. `index.html` keeps the name `SCENARIOS` and reads `window.SCENARIOS`. | The old shape said nothing about which surfaces a scenario meant anything on, hid the seed inside the code that applied it, and could not be read by the console at all. Adding a twelfth scenario is now a row of data rather than a `case`. Verified behaviour-preserving: All eleven original scenarios were captured before and after on hash, order status, payment status, display-status key, pass count, issued count, admissions, totals, paid amount, inquiries, draft, heading, and full visible copy. Every field identical. | Yes. |
| 52 | 4a | Three screens stopped branching on a label string | `S.scenario` holds the key. `scrOpenPlay`, `scrVisit`, and `scrCourtWhen` compare `S.scenario === "empty"` and `=== "loading"` where they compared `S.injected === "No Open Play scheduled"` and `=== "Loading availability"`. `S.injected` still holds the label, for the banner only. | Rewording a scenario label was a silent behaviour change: three screens would quietly stop reacting and nothing would fail. This is the specific fragility that let the fourth comparison rot unnoticed. See row 53. | Yes. |
| 53 | 4a | The dead `Inventory conflict` branch was revived rather than deleted, as a twelfth scenario | `scrCourtReview` had `const conflict = S.injected === "Inventory conflict"` guarding a written, styled banner. No scenario has ever had that label, so the branch was unreachable. It is now `S.scenario === "conflict"` and `conflict` is a real scenario landing on `/court/review`. | Beyond what the prompt asked, and recorded as such. The alternative was deleting an unreachable branch, which would have removed a designed product state ("That time was just taken. Choose another available time.") whose copy was already written and reviewed. Reviving it costs one row of data and turns dead code into something a reviewer can press. It does not change any of the eleven existing scenarios. The scenario count in the `/demo` copy is computed from the array, so it updated itself. | Yes. |
| 54 | 4a | The magic `50` in the mismatch seed was named | `MISMATCH_SHORTFALL = 50`, declared beside the rates it is short of. | It was `orderTotal(o) - 50` inside the switch, and 50 pesos was doing real work: Small enough to read as a fat-fingered transfer rather than a different order, which is the judgement staff actually have to make. | Yes. |
| 55 | 4b | The console hydrates from `data-scn` hooks, not from a template system | `admin.js` gained `applyScenario()`. It reads `STAGING.getScenario()`, finds the scenario's `admin.pages[page]`, and does three things: Sets the text of marked spans, injects one marked row into one marked slot per screen, and puts a hatched marker under the app bar. Eight pages gained one `data-scn-slot`; `dashboard.html` gained four `data-scn` spans. | The prompt asked for a light pass and this is the lightest shape that still produces correct state. Twenty-six screens of sample markup is what makes the console reviewable at all; converting them into a renderer would trade that for a data model nobody asked for. Page identity comes from the filename rather than `data-nav`, because Settings and Tasks both declare `data-nav="more"` and a scenario has different things to say about each. | Yes. |
| 56 | 4b | Where a scenario means nothing, the console says so | A screen with no entry for the armed scenario renders "No consequence on this screen. The sample data below is unchanged and still true." | The sandbox-honesty rule applied to state rather than to controls. Leaving the sample data standing silently is indistinguishable from a bug, and is exactly the "sample data that has become untrue" the prompt names. Stating it is more useful to a reviewer than either silence or an invented consequence. | Yes. |
| 57 | 4b | The happy path renders no marker on either side | `applyScenario()` returns early on `scn.natural`, and `happy` carries `admin: null`. | The customer build has always shown no hatched marker for `happy`, because it is not a forced state. The console agreeing costs nothing and disagreeing would have meant the same scenario looking forced on one surface and natural on the other. `happy` is also the one scenario with nothing to inject: A confirmed booking and a clean queue is the console's resting state, and announcing it would be the console narrating itself. | Yes. |
| 58 | 4b | Two scenarios carry a second, quieter band | `offline` and `loading` add `.injected--band` under the marker, saying the whole screen is stale or unconfirmed rather than one row of it. | Both are about the availability service rather than about a booking, so the honest statement is about everything below, not about an injected row. Separating it from the hatched marker keeps the marker's job single: What is forced, versus what that means for this data. | Yes. |
| 59 | 4b | `.injected` was added to `admin.css`, and `flask` to the console's icon sprite | Same hatching and same glyph as the customer build's marker, in the console's tokens. | The marker existed only in `index.html`. `flask` was the one glyph the marker needed that `admin.js` did not have, which rendered as a hole in the band rather than as an error, so it was caught by looking rather than by testing. Marker placement is inside `#main` as its first child, pulled out to the gutters: The desktop grid pins the app bar to row 1 and `#main` to row 2, so a third element between them would have landed on top of one of them. | Yes. |
| 60 | 4b | Two `set` keys that had nowhere to write were removed rather than hooked | `full` and `empty` each declared `op-registered` and `op-waitlist` overrides for `open-play.html`. No such hooks exist, so the applier silently skipped them. | A stub that does nothing is the thing this project's own rules forbid, and it does not stop being one because it lives in a data file rather than on a button. Hooking them was the alternative and was rejected: The Open Play page's first session already reads 8 of 8 and Full, so `full` had nothing to change, and writing "No window published" into a field labelled Registered is worse than the injected row that says it plainly. | Yes. |
| 61 | 4c | Admin is the console view of `opsmanager`, not a fifth surface | The Surface row gained a full-width "Admin / Club Manager, opens the console" control. It sets `S.surface = "opsmanager"`, writes the dials, and navigates to `admin/dashboard.html`. It carries no `aria-pressed`. | Rea Salvador is the Club Manager and `opsmanager` already exists. A fifth surface would mean inventing a fifth set of permissions for the same person and keeping the two in step forever. The control has no pressed state because the four above it are states this document can be in and this one is a door; a toggle that can never read as pressed is a control lying about itself. The harness copy says the same thing in a sentence, and `ALLOWED.surface` in the bridge still has four values, not five. | Yes. |
| 62 | 4 | Phase 4 added no drift to the default state | The mobile diff after Phase 4 is identical to the diff after Phase 3, apart from `app__demo` growing by the new surface control and scenario copy. | Worth recording because it is the design working: The console renders exactly as it did when no scenario is armed, and everything the hydration does is visible only while one is. A reviewer opening the console cold sees the reviewed sample data, not a hydrated approximation of it. | n/a |
