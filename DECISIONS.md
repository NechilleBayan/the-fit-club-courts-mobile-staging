# Decision log: Admin console integration

Append-only. Newest last. One row per decision, and a decision is anything that
removed, merged, renamed, reassigned, or overrode something that existed before.

The test this log has to pass: Someone reading only this file can find anything
that disappeared, and can tell a deliberate call from an accident.

Where the prompt was silent and judgement was exercised, the row says so and
names the alternative that was rejected.

| # | Phase | Decision | What changed | Why | Reversible? |
|---|---|---|---|---|---|
| 1 | 0 | Baseline is commit `f46784e`, not `60399fb` | Branch `admin-console-integration` cut from `main` at `60399fb`. The `admin/` folder was untracked on `main`, so it was committed verbatim as `f46784e` before any edits. | A 27-file folder that git has never seen produces no diff. Every later change to `admin/` would have arrived as an undifferentiated block of new files, which makes the branch unreviewable. `f46784e` contains zero edits, so it is a true baseline. | Yes. `git diff f46784e -- admin/` is the whole of this branch's admin work. |
| 2 | 0 | Visual baselines are gitignored, not committed | Added `.baseline/` to `.gitignore`. 104 PNGs live on disk at `.baseline/{mobile,desktop}/` for the run. | The prompt allowed either. 104 binaries, regenerated at every checkpoint, would dominate the history of a branch whose readable diff is the deliverable. Rejected alternative: Commit them and accept the noise. | Yes. Delete the ignore line and `git add .baseline`. |
| 3 | 0 | Screenshot runner lives outside the repo | `shoot.mjs` plus its `playwright` install sit in the session scratchpad, not in the working tree. | The prototype's stated identity is no npm, no build step, no dependencies. Putting a `package.json` and a `node_modules` at the repo root to verify that claim would falsify it. The runner is verification tooling for this branch, not part of the prototype. Rejected alternative: A `tools/` folder in the repo with its own `package.json`. | Yes, and the script is reproduced in `ADMIN-INTEGRATION.md` so the run can be repeated. |
| 4 | 0 | Unreferenced carousel images left untracked | `carousel/c19.jpg` through `c37.jpg` remain untracked and uncommitted. | Nothing in `index.html`, `admin/`, or `preview.html` references them. They are out of this branch's scope and committing them would be an unrelated change riding along. Rejected alternative: Commit them for tidiness. | Yes. They are still on disk. |
| 5 | 0 | Prompt fact corrected: The staff shell does have desktop navigation | No code change. Recorded so Phase 2 is not built on a wrong premise. | The prompt states the staff shell has no desktop navigation above 1024 and that a Front Desk user gets the customer header as their only nav. Neither is true. `render()` populates `#bar-nav` and `#tabhost` from the same `STAFF_TABS` array on the staff branch, so the header carries Today, Scan, Walk-in, Open Play, and Exceptions with `aria-current` on the live one, and the 1280 baseline of `/staff/today` shows it. The comment above that branch is an explicit prior decision: "Staff keep theirs ... It is a different application wearing the same chrome." The Phase 2 sidebar therefore replaces a working, deliberately chosen desktop nav rather than filling a hole, which is a stronger claim than the prompt assumed and is settled in row 6. | n/a |
| 6 | 1 | The persisted key is `tfc-staging`, holding exactly six scalars | `{ surface, scenario, connectivity, capacity, clockOffset, theme }`, one JSON object, validated on every read. `S.orders` is not in it and never will be. | An order graph carries absolute hold deadlines, generated identifiers, and a share token per pass, and about thirty functions in `index.html` mutate it. Moving it across a document boundary means freezing all of that into a wire format and keeping two renderers honest about it forever. The console gets a scenario key and draws its own sample instead. | Yes. One file, one key. |
| 7 | 1 | Theme defaults to `null`, not to a colour | `getTheme()` returns `null` until somebody chooses. Each build passes its own fallback to `applyTheme()`: The customer build passes `dark`, the console passes `system`. | Picking a single global default would have repainted one of the two builds for a reader who never asked. The customer build ships `<html data-theme="dark">`; the console ships System selected and renders light. Either choice regresses 27 or 25 baseline screenshots on day one. `null` means "nobody has chosen, keep your own default", so both baselines are preserved and an explicit choice still crosses. Rejected alternative: Default everything to dark. | Yes. |
| 8 | 1 | The old `tfc-theme` key is migrated and then deleted | First read adopts a `dark`/`light` value from `tfc-theme`, writes it into `tfc-staging`, and removes the old key. | Keeping both would be two sources of truth for one setting, which is the exact thing this phase exists to remove. Migrating preserves the reader's current preference, which is the other half of the requirement. Rejected alternative: Keep mirroring writes to `tfc-theme` for backward compatibility. Note the consequence: `claude-version-index.html`, the superseded snapshot, still reads `tfc-theme` and will fall back to its own pinned default. It is a frozen snapshot and out of scope. | Yes, but the old key is gone once a browser has loaded the new build. |
| 9 | 1 | Behaviour change: Reset all state now clears the theme | It previously did not. The comment in `index.html` called the theme "the one thing in this build that outlives a refresh". | Checkpoint 1 asks that Reset return both surfaces to default, and a reset that leaves the console painted in a choice made twenty minutes ago in the other build is not a reset. The old comment has been rewritten to say the new rule rather than left to contradict the code. | Yes. Exclude `theme` from `STAGING.reset()`. |
| 10 | 1 | The armed scenario is remembered but not replayed in the customer build | `loadScenario()` writes the key. Boot restores surface, connectivity, capacity, and clock offset, but does not re-run the seeding. The harness marks the armed scenario with `aria-pressed` so it is still visible. | Replaying on load would mint a fresh order with a new identifier and a new hold deadline while the address bar still pointed at the order the previous run created, which is a 404 dressed as a feature. Dials persist; bookings do not. Rejected alternative: Re-seed on boot and rewrite the hash. | Yes. |
| 11 | 1 | Deviation from the prompt: The bridge is a `<script>` tag in each admin page's head, not a dynamic load from `admin.js` | 27 admin pages gained `<script src="../staging-state.js" data-default-theme="system">` in the head, ahead of the stylesheet. `admin.js` subscribes but does not load the file. | The prompt said to load it from `admin.js`. `admin.js` is at the foot of the body, so anything it loads runs after the first paint, and the first thing this file does is settle the theme. Loading it there is a guaranteed flash of the wrong theme on every one of 27 pages. The default is declared on the tag itself so each document names its own fallback in one place. | Yes. |
| 12 | 1 | `index.html` is no longer a single self-contained document | It now loads one external script, `staging-state.js`, from its own origin. | It already loaded 18 carousel JPEGs and ten font files, so this is not the first request, but it is the first script. The alternative was pasting the same 250 lines into both builds, which is two sources of truth for the thing whose whole purpose is to be one. Consequence: Opening `index.html` over `file://` now depends on the browser allowing a sibling script and an origin-null localStorage. The project already specifies a local server. | Yes. |
| 13 | 1 | `ACTIONS.resetall` split into `resetRuntime()` plus `STAGING.reset()` | `loadScenario()` now calls `resetRuntime()` rather than `ACTIONS.resetall()`. | Loading a scenario has to start from a clean runtime but must not throw away the reader's theme, which now lives in the persisted half. Only the button that says it resets everything clears the key. Side effect: `loadScenario()` no longer fires an intermediate `go("/")`. That was invisible, because `go()` sets `location.hash` and every scenario navigates again before the resulting `hashchange` fires, so only the final route was ever rendered. | Yes. |
| 14 | 1 | `subscribe()` callbacks receive a third argument, `origin` | `"local"` for a change made in this document, `"remote"` for one that arrived by storage event. | Without it, a control that has already updated its own runtime and rendered gets told to do it again on its own echo, so every tap renders twice. The prompt specified a change-subscription hook and left the shape open. Also added: Setters for connectivity, capacity, and clock offset, which the prompt listed only as getters, because the harness has to write them. | Yes. |
| 15 | 1 | Copy rewritten in three places, because it had become untrue | `openDemo()`'s intro, `scrDemo()`'s lede plus a new "What survives a refresh" section, and `admin/settings.html`'s "nothing is saved" sandbox note. | All three promised that nothing persisted. The theme picker in Settings, in particular, sat under a note saying nothing is saved while being the one control that now genuinely saves. The sandbox-honesty rule makes fixing this mandatory rather than optional. | Yes. |
| 16 | 1 | Verification harness: Pixel diff with a stated noise floor, not byte equality | Screenshots freeze the clock, force every declared font face to load, decode every image, and disable animation and transition before capture. Comparison is `pixelmatch`, reporting differing pixel counts. | Byte equality produced 11 false positives out of 104 against an unchanged tree. The largest cause was the 18 grayscale film-strip frames carrying `decoding="async"`: An image can report `complete` and still be undecoded at capture. The second was the split latin / latin-ext fonts, where the peso sign at U+20B1 starts a second font fetch only after `document.fonts.ready` has already resolved, worth 22 pixels of reflow. Residual noise floor after all of that: `desktop/app__court` and `desktop/app__event` still flip height occasionally ON THE BASELINE TREE, by 1px and 23px. Both were confirmed to flake on `f46784e` with no changes applied, so neither is attributable to this branch. | n/a |
| 17 | 2 | The collision is resolved in the sidebar's favour: The staff shell gives up its header nav | At 1024px and up, `body.has-sidenav .topnav{display:none}`. Staff destinations move from the two strip header into the rail. The customer path is untouched at every width. | Row 5 established that the staff header nav already worked and was deliberate. It is replaced anyway, because the split that matters is product, not width. A visitor reads down a page toward one conversion action; a Front Desk staffer moves between five destinations with no hero and nothing to convert. The second is a console, and it is the same console `admin/` is, so it wears the same rail. The prior comment's own premise, that staff are "a different application wearing the same chrome", is what argues for this: Staff being a different application was right, and the conclusion has moved from same chrome to its own. Rejected alternative: Leave the header nav alone and give the rail only to `admin/`. Cheaper, regresses nothing, and turned down because it leaves two staff-facing desktop chromes in one product. Customer differing from staff is a distinction a reader learns once. Front Desk differing from Ops Manager is one nobody can. | Yes. Delete one CSS rule and one `paintSidenav` call. |
| 18 | 2 | `TABS` and `DRAWER` moved out of `admin.js` into a new `console-nav.js` | `admin.js` now reads `window.CONSOLE_NAV`, keeping the old local names so the rest of the file reads as it did. `DRAWER` is derived by flattening the grouped model, which is the single sequence the dialog has always rendered. | Two documents now draw the same destination list. A second copy is a second place to add a screen, and therefore a place to forget to. Also moved: The sidebar renderer itself, so the two builds cannot drift on the current-page marker either. | Yes. |
| 19 | 2 | The sidebar's stylesheet is shared too, in `console-nav.css`; the layout around it is not | Component rules live in `console-nav.css`, loaded by both builds. Shell layout stays per document: The grid in `admin.css`, the fixed offset in `index.html`. | The rail is the same rail on both sides, so drawing it twice risks the same divergence a duplicated destination list does. The shells are genuinely not alike, so their layouts stay separate. The shared sheet names no colours, only tokens, which both builds declare; that is what lets one file serve two palettes that are inverses of each other. `.countpill` is redeclared inside it because `admin.css` had it globally and the customer build had it nowhere, so the component would have rendered unstyled on one side. | Yes. |
| 20 | 2 | The customer build uses a fixed rail with a padding offset; the console uses a grid column | `body.has-sidenav #shell{padding-inline-start:var(--sidenav-w)}` plus `position:fixed` on the rail, versus `#shell{display:grid}` in the console. The rail element is also inserted as a sibling of `#shell` rather than a child. | The console owns a plain `#shell` it can turn into two columns. The customer `#shell` is a flex column carrying a sticky header group, a full bleed hero container, and a fixed dock, and re-parenting all of that into grid tracks to move a navigation is a far larger change for no more result. It also carries `overflow-x:clip`, and a fixed element inside a clipping ancestor is a bet on which browser is reading the spec that week, hence the sibling placement. Rejected alternative: Force the grid on both for symmetry. | Yes. |
| 21 | 2 | The rail follows the path, not the shell: `body.has-sidenav`, not `body.shell-staff` | `paintSidenav` shows the rail when `view.shell === "staff"` OR the path starts with `/staff/`. | The prompt listed five staff routes. Two of them, `/staff/walkin` and `/staff/openplay/:id`, return the TASK shell, not the staff shell, and so have never had the mobile tab bar either. Keying on the shell would have left them railless and jumped the whole page 264px sideways every time a staffer started or finished a walk-in. The reason a phone withholds navigation on a task screen is vertical budget, and that constraint does not exist at 1280px. So the two renderings of one nav model legitimately disagree about task screens. `/staff/booking/:id` is included by the same rule. Rejected alternative: Match the mobile rule exactly and accept the jump. | Yes. |
| 22 | 2 | The persisted shape gained a seventh field, `navCollapsed` | `{ surface, scenario, connectivity, capacity, clockOffset, theme, navCollapsed }`. | Phase 1 documented the shape as exactly six scalars, and this breaks that. The prompt requires the collapse choice to persist via `STAGING`, and the field is the same category as `theme`: A preference about chrome rather than a harness dial. The file header now says six dials plus one chrome preference rather than pretending it is seven of a kind. | Yes. |
| 23 | 2 | Front Desk does not see the Money group, and the rail says so | `CONSOLE_NAV.groupsFor(surface)` returns the money group only for `opsmanager`. The staff rail carries a note naming the absence. | The permission boundary between Ops Manager and Front Desk is the load-bearing distinction in this product and the exception queue already draws it. A rail that silently omits three rows teaches nothing; one that says the money area is the Operations Manager's, and that the server denies it too, demonstrates the boundary. The test is an ALLOW, not a deny: The first version listed the surfaces refused, which admitted the harness's own `visitor` by default and showed revenue reports to nobody in particular on a cold start. | Yes. |
| 24 | 2 | The Session group is dropped from the staff rail; the console keeps it | `paintSidenav` filters out `Session`. | There is no session in the customer build to end. The surface is a harness dial, not a login, so a Log Out row there would be the first control in the build that does nothing, which the sandbox-honesty rule forbids. The console has an identity to sign out and keeps its row. | Yes. |
| 25 | 2 | Seven glyphs added to the customer build's sprite, at that sprite's stroke weight | `building`, `key`, `card`, `chart`, `bell`, `activity`, `settings` added as `<symbol>`s in `index.html`, path data lifted from `admin.js`. A `NAV_GLYPH` table maps the model's names onto this sprite's existing ids (`calendar` to `cal`, `user` to `person`, `hours` to `clock`, `help` to `info`). | The shared model names glyphs in the console's vocabulary and this sprite was missing seven of them. Same path data, so the two builds draw the same bell rather than two bells. Stroke weight is this file's 2 rather than the console's 1.75, because a symbol wearing the console's weight would be the one icon in the document drawn lighter than its neighbours. That the two sprites disagree on weight AND on mechanism (`<symbol>` plus `<use>` versus path strings) is noted here for Phase 3c to settle; it did not need settling to render a navigation. | Yes. |
| 26 | 2 | The pending-tasks count uses the quiet pill, not the red one | `quiet:true` on the More row; Notifications keeps `--critical`. | The console's own token comment reserves its only red for the unread badge and for states that cost money or close a court. Five tasks sitting in a queue is neither. The collapsed rail follows the same rule: A quiet count becomes a grey dot, not a red one. | Yes. |
| 27 | 2 | The rail identity shows role only, not role plus club | `head.sub` is `DATA.admin.role` in the console, `VENUE.name` in the staff shell. | The drawer can afford both because its panel is 19rem of a phone screen with nothing beside it. The rail is narrower and the club name is the half that truncates, leaving "Club Manager, The F..." saying less than "Club Manager" does. | Yes. |
| 28 | 2 | Collapsed labels are clipped, not removed | `opacity:0; width:0; flex:0 0 0` rather than `display:none`. | `display:none` removes the text from the accessibility tree along with the layout, and every row in the icon rail would then announce as a link with no name. Clipped, the label is still the row's accessible name. Rejected alternative: An `aria-label` on every row, which duplicates the visible text when the rail is expanded. | Yes. |
| 29 | 2 | The drawer is closed when a narrow window is widened past 1024 | A `matchMedia` listener in `admin.js` closes `#drawer` on the way up. | The drawer and the rail are the same navigation at two widths and must never both be present. Neither stylesheet can close a dialog that is already open, so a reader who opens the drawer at 900px and drags the window to 1200px would otherwise be looking at a modal sitting over a navigation that says the same thing. | Yes. |
| 30 | 3a | The roster becomes a real array in `admin.js`, and `pendingTasks` is derived from it | `DATA.staff` holds the two people with their per-status task counts, `DATA.unassigned` holds the unowned work, and `DATA.pendingTasks` and `DATA.onDuty` are computed rather than typed. `DATA.admin` is now `STAFF[0]` rather than a separate literal. | The file's stated premise is that the badge, the drawer count, and the dashboard figure cannot disagree. A hand-typed `pendingTasks: 5` beside a hand-edited queue can, and after moving five tasks between three owners it very nearly did. Deriving it makes the disagreement impossible rather than merely unlikely. | Yes. |
| 31 | 3a | Removed: Paolo Reyes, Maintenance, 18 references across 12 files | `activity.html` (completed lighting check) to Rea. `bookings.html`, `courts.html`, `court-details.html` x2, `dashboard.html`, `maintenance.html` x2, `notifications.html` to **Unassigned** where the reference was live work. `court-details.html` and `maintenance.html` timeline entries (3) to Rea, because a log records what happened and "Unassigned did this" is a lie about the past. `staff.html` and `staff-manage.html` rows deleted. `tasks.html`: The assignee option deleted, two open tasks to Unassigned, one completed task to Rea. | Maintenance is the role with nobody in it, so its live work is genuinely unowned and Unassigned is the honest state rather than a gap. The split between live work and log entries is the rule applied throughout: A future or in-flight job can have no owner, a completed one cannot. | Yes. |
| 32 | 3a | Removed: Carlo Santos, Coach, 6 references across 5 files | `open-play.html` Coach field to **Unassigned**. `check-in.html` Beginner Clinic host to Rea. `tasks.html`: The assignee option deleted, and "Confirm Sunday coaching slots" reassigned to Rea. `staff.html` and `staff-manage.html` rows deleted. | The club has no coach on staff, so the coach slot on a session is empty and says so. The clinic still has to be run by somebody today, and confirming outside coaching is a manager's job, so those two went to Rea rather than into the void. | Yes. |
| 33 | 3a | Removed: Mika Dizon, Front Desk, 4 references across 4 files | `staff-details.html` sentence rewritten (see row 36). `tasks.html` assignee option deleted. `staff.html` and `staff-manage.html` rows deleted. | Mika held nothing: The row said "No tasks" and the only prose reference existed to point at that emptiness. Nothing needed reassigning. | Yes. |
| 34 | 3a | Removed: Bea Lorenzo, Front Desk, 4 references across 2 files | `staff.html` row deleted, and the Shift Unfilled plate rewritten (row 37). `tasks.html`: The assignee option deleted, and "Wipe down seating area" reassigned to **Unassigned, Evening desk shift**. | Bea was the evening desk cover. With her gone the evening shift is genuinely unfilled, which the console already had a plate for and which is better demo material than a name. Her one task follows the shift rather than a person. | Yes. |
| 35 | 3a | Removed: Aldrin Torres, Coach, and Nico Garcia, Maintenance, 1 reference each, both in `staff.html` | Both rows deleted outright. | Neither appeared anywhere else in 27 files. They were list padding: An off-duty coach and a maintenance staffer on leave, existing to make a roster look longer. Nothing to reassign and nothing lost. | Yes. |
| 36 | 3a | `staff-details.html` stopped pointing at somebody with spare capacity | "Consider giving the next job to Mika Dizon, who has none" became "There is nobody to hand the next job to: Rea is carrying two of her own and the evening desk shift is unfilled." | The sentence existed to show a manager the obvious next move. With a cast of two there is no obvious next move, and saying so is the more useful screen: The console's job here is to show that the club is at capacity, not to invent a name with slack. | Yes. |
| 37 | 3a | The Shift Unfilled plate now says nobody, not a name | "Bea Lorenzo covers to 10:00 PM but is alone on the desk" became "Unassigned. Both staff finish by 3:00 PM, and Evening Open Play runs 7:00 PM to 11:00 PM across all four courts." | The plate was already flagging a cover problem and now flags a starker one, which is the correct reading of a two person roster that ends at 3:00 PM against a session that runs to 11:00 PM. It also ties the console's staffing to the customer build's Open Play window times for the first time. | Yes. |
| 38 | 3a | Counts updated wherever the cull moved them | `staff.html`: On Duty Now 5 of 8 to 2 of 2; "8 Staff Members" to "2 Staff Members"; Joy's chip 3 tasks to 2. `staff-manage.html`: "8 Accounts" to "2 Accounts"; role tallies 2/3/3 to 1/1/Nobody; "3 more accounts" line deleted. `dashboard.html`: Staff On Duty 5 to 2; "waiting on 3 staff members" to "Three have an owner, two do not." `tasks.html`: Lede and assignee options. `staff-details.html`: Joy's "3 open tasks" to 2. | These are the figures the prompt named. `unread` (4) and the 9 completed tasks are unchanged, because neither depends on how many people are on the roster. | Yes. |
| 39 | 3a | Pre-existing bug fixed: Overdue was a fourth bucket that nothing counted | `tasks.html` lost one Pending row ("Print weekly rate card for the desk", Joy), and the queue comment now states that Overdue is a late Pending rather than a stage of its own. | Found while reconciling counts, not introduced by it. The queue showed 5 rows tagged Pending PLUS 1 tagged Overdue, while `DATA.pendingTasks`, the dashboard, and the staff sumcard all said 5 pending with 1 overdue among them. The segmented control's "All 16" equalled 5 + 2 + 9, which excluded the overdue row entirely, so the console disagreed with itself by one in whichever direction a reader added it up. Now: Pending is 5 with 1 late, Active 2, Done 9, All 16. The deleted task was also removed from Joy's detail page, where it was listed a second time. | Yes, but it would restore an inconsistency. |
| 40 | 3a | The Off Duty filter was removed from `staff.html` | One `<button class="filter">` deleted, with a comment saying why. | Both remaining staff are on shift, so the filter selected an empty list every time it was pressed. The filters in this sandbox only paint, so it would not even have looked empty; it would have looked broken. The three that remain (All, On Duty, Has Pending Tasks) all describe a set that exists. | Yes. |
| 41 | 3a | Name collision resolved by the cull, and the player keeps the name | No change to `index.html`. `Mika` remains an Open Play player in three places: The `names` scenario seed, the Open Play roster sample, and "Mika R." in the Today arrivals list. | The collision was between a player called Mika and a Front Desk staffer called Mika Dizon. Mika Dizon is gone from all 27 console pages, verified by rendering each one and matching the text, so no screen shows the player while a staffer of that name exists. The arrivals entry is explicitly a customer arriving at the desk. Renaming the player as well would be a change to the customer build's reviewed sample data for a collision that no longer exists. | n/a |
| 42 | 3b | The contact sheet is gone; `admin/index.html` is now a redirect | The 26-row hand-maintained index was deleted. What replaces it is a three-line redirect to `dashboard.html`: `location.replace` first so the page never enters history and Back does not bounce, a `meta refresh` for scripting off, and a real sentence with a link for both off. | The filename has to keep existing, because a request for `admin/` with no document resolves to it and a static server would otherwise answer with a directory listing or a 404. Neither is a sensible landing. The dashboard is, which is what the prompt suggested and what this does. `replace` rather than `href` is the part worth noting: `href` would have put the redirect in the history and made Back unusable. | Yes, the old sheet is in `git show 01634ae:admin/index.html`. |
| 43 | 3b | All 26 console screens joined `ALL_ROUTES`, not a second array | Each entry is `[href, "adm-id, Label", group]`. The third element is the group heading and doubles as the flag that says href rather than hash. The renderer emits a heading when the group changes, drops the `#`, and drops `data-action="closesheet"` because the document is leaving anyway. | One array, because the whole reason the contact sheet had to go was that two hand-maintained indexes of one folder will disagree and the second one edited will be the wrong one. Putting the console in its own array would have rebuilt that problem inside one file. Ids follow the existing spirit: `adm-dashboard`, `adm-bookings`, and so on, 26 of them, all verified to resolve. | Yes. |
| 44 | 3b | The way back to the customer build lives in the System group | A `Customer Build` row was added to the shared `GROUPS`, marked `absolute` so the `admin/` prefix is not applied and `consoleOnly` so it never renders in the staff shell. It appears in the console's desktop rail and in its mobile drawer. | The old contact sheet held the only link back to `../index.html`, and deleting it would have made the console a folder you can enter and not leave except through the address bar. `consoleOnly` exists because the staff rail is already inside the customer build, where a link to the document you are reading is furniture. `groupsFor()` strips those rows for every caller, since its only caller is the customer build; the console reaches for `GROUPS` directly. | Yes. |
| 45 | 3c | Two builds described two different clubs. The customer build won | `club.js` is new and loaded by both. Address, phone, email, maps link and courts come from it. The console's Kapitolyo, Pasig address, its `+63 2 8555 0140` landline, and its `hello@thefitclubcourts.ph` address are gone from `admin.js`, `club-profile.html`, and `help.html`. `index.html`'s `VENUE` is now `window.CLUB` and `MAPS_URL` reads from it. `joy.m@thefitclubcourts.ph` became `joy.m@example.com`, matching the convention every other sample address in the build already used. | Not a wording difference: Silway-8, Polomolok, South Cotabato and Kapitolyo, Pasig City are 1,200 kilometres apart, in one prototype. The customer build wins because its address is load bearing, being what the Google Maps link resolves to and what a visitor is told to travel to, and because `thefitclubcourts.ph` was a domain nothing else in the project used. | Yes. |
| 46 | 3c | The hours argument went the other way, and the Monday closure was deleted | The club is open daily, 6:00 AM to 11:00 PM. `hours.html`'s seven rows are identical and say why. The weekly Monday closure is gone from `closures.html` (replaced by a "No recurring closures" empty state), from `reports.html` (Monday is now the quietest open day, bar and caption rewritten), from `club-profile.html`, and from `dashboard.html`. Two one-off Monday closures in `closures.html` and `court-details.html` kept their dates and were re-reasoned as a surface sweep. | The console had the more detailed story and it was the wrong one. It also contradicted the customer build's functional data: `OP_WINDOWS` sells Open Play from 7:00 PM to 11:00 PM, which is an hour past a 10:00 PM close. Hours the club is shut cannot be hours it is selling. Functional data beats the copy describing it, so the copy moved. Rejected alternative: Cut the Open Play window to 10:00 PM, which is a change to reviewed customer behaviour to preserve a sentence. Correction to an earlier draft of this row, recorded rather than edited away: It also claimed the customer build offers seven consecutive dates with no exclusions. It does not. `scrCourtWhen` has `closedDay = d.dayIndex === 3`, a closed day at a rolling three-day offset from today. That is sample data demonstrating the closed-day state, and because it rolls it can never line up with a fixed weekday, so it still cannot be the console's every-Monday closure. The Open Play argument above is the one that carries the decision. | Yes, but it would restore the contradiction. |
| 47 | 3c | Status vocabulary: `Reserved` became `Held`, and the console gained `Needs review` | Three booking rows in `dashboard.html`, `courts.html`, and `court-details.html`. The legend in `help.html` now reads Held rather than Reserved, adds Needs review to the red row, and carries a paragraph saying the words are the customer build's words. | `Reserved` and `Held` name one state, and they do not mean the same thing to a reader. The customer build spends real copy insisting this state is "Not booked yet" and that the time is released unless payment is verified; `Reserved` says the opposite in one word. `Needs review` was missing entirely, while the customer build's whole exception queue exists to close that loop and one of the eleven scenarios is called `mismatch`, so the console had no word for a state Phase 4 has to render. | Yes. |
| 48 | 3c | Deliberately left alone: `Paid` and `Unpaid` | Eleven and four uses, unchanged. | They look like they clash with `Confirmed` and `Awaiting payment` and they do not: They are a different axis. In this product a payment can arrive and NOT confirm an order, which is exactly what the amount-mismatch path is for, so paid and confirmed are genuinely separate facts about one booking and the console showing both is right. What IS a real divergence is underneath them: The console models bookings that sit unpaid for days ("PAYMENT OVERDUE, 3 days late") while the customer build releases an unpaid hold in about ten minutes. Reconciling that is a product decision about whether this club invoices, not a copy fix, and it is left open here on purpose so nobody has to rediscover the question. | n/a |
| 49 | 3c | Deliberately left alone: The two icon sprites are not merged | `index.html` keeps 57 `<symbol>` elements referenced by `<use>` at stroke weight 2. `admin.js` keeps 48 path strings injected inline at weight 1.75. | They are the same drawing language, which Phase 2 proved by lifting seven glyphs from one to the other unchanged, but they are not the same mechanism. Merging means either injecting an SVG sprite into 27 static documents and rewriting `svg()`, or converting 57 symbols to path strings and rewriting `icon()`. Both touch how every icon on 51 routes and 26 screens renders, both risk the mobile baselines this branch exists to protect, and neither changes a single pixel if done correctly. That is a large blast radius for zero visible gain. What was worth doing is done: Where the two overlap, they now draw the same glyph. The stroke-weight difference (2 against 1.75) is real and visible if the two builds are put side by side; it is left because normalising it would repaint every icon in one of them. | n/a |
| 50 | 3c | Deliberately left alone: Customer initials and player names in the console | `AL`, `DO`, `GV`, `JB`, `KU`, `LR`, `MT`, `NC`, `RA` and their full names stay. | The cull was of the STAFF cast, which is the one that carries the permission boundary. Customers are the club's demand and a console with two of them would demonstrate less, not more. They also do not appear in the customer build, so there is no second copy to drift from. | n/a |
| 51 | 4a | `SCENARIOS` moved to a shared `scenarios.js` and became a model rather than a list of labels | Each entry now carries key, label, blurb, `surfaces`, a `seed`, and an `admin` block. `loadScenario()` reads the seed through four small helpers instead of a thirty-line switch. `index.html` keeps the name `SCENARIOS` and reads `window.SCENARIOS`. | The old shape said nothing about which surfaces a scenario meant anything on, hid the seed inside the code that applied it, and could not be read by the console at all. Adding a twelfth scenario is now a row of data rather than a `case`. Verified behaviour-preserving: All eleven original scenarios were captured before and after on hash, order status, payment status, display-status key, pass count, issued count, admissions, totals, paid amount, inquiries, draft, heading, and full visible copy. Every field identical. | Yes. |
| 52 | 4a | Three screens stopped branching on a label string | `S.scenario` holds the key. `scrOpenPlay`, `scrVisit`, and `scrCourtWhen` compare `S.scenario === "empty"` and `=== "loading"` where they compared `S.injected === "No Open Play scheduled"` and `=== "Loading availability"`. `S.injected` still holds the label, for the banner only. | Rewording a scenario label was a silent behaviour change: Three screens would quietly stop reacting and nothing would fail. This is the specific fragility that let the fourth comparison rot unnoticed. See row 53. | Yes. |
| 53 | 4a | The dead `Inventory conflict` branch was revived rather than deleted, as a twelfth scenario | `scrCourtReview` had `const conflict = S.injected === "Inventory conflict"` guarding a written, styled banner. No scenario has ever had that label, so the branch was unreachable. It is now `S.scenario === "conflict"` and `conflict` is a real scenario landing on `/court/review`. | Beyond what the prompt asked, and recorded as such. The alternative was deleting an unreachable branch, which would have removed a designed product state ("That time was just taken. Choose another available time.") whose copy was already written and reviewed. Reviving it costs one row of data and turns dead code into something a reviewer can press. It does not change any of the eleven existing scenarios. The scenario count in the `/demo` copy is computed from the array, so it updated itself. | Yes. |
| 54 | 4a | The magic `50` in the mismatch seed was named | `MISMATCH_SHORTFALL = 50`, declared beside the rates it is short of. | It was `orderTotal(o) - 50` inside the switch, and 50 pesos was doing real work: Small enough to read as a fat-fingered transfer rather than a different order, which is the judgement staff actually have to make. | Yes. |
| 55 | 4b | The console hydrates from `data-scn` hooks, not from a template system | `admin.js` gained `applyScenario()`. It reads `STAGING.getScenario()`, finds the scenario's `admin.pages[page]`, and does three things: Sets the text of marked spans, injects one marked row into one marked slot per screen, and puts a hatched marker under the app bar. Eight pages gained one `data-scn-slot`; `dashboard.html` gained four `data-scn` spans. | The prompt asked for a light pass and this is the lightest shape that still produces correct state. Twenty-six screens of sample markup is what makes the console reviewable at all; converting them into a renderer would trade that for a data model nobody asked for. Page identity comes from the filename rather than `data-nav`, because Settings and Tasks both declare `data-nav="more"` and a scenario has different things to say about each. | Yes. |
| 56 | 4b | Where a scenario means nothing, the console says so | A screen with no entry for the armed scenario renders "No consequence on this screen. The sample data below is unchanged and still true." | The sandbox-honesty rule applied to state rather than to controls. Leaving the sample data standing silently is indistinguishable from a bug, and is exactly the "sample data that has become untrue" the prompt names. Stating it is more useful to a reviewer than either silence or an invented consequence. | Yes. |
| 57 | 4b | The happy path renders no marker on either side | `applyScenario()` returns early on `scn.natural`, and `happy` carries `admin: null`. | The customer build has always shown no hatched marker for `happy`, because it is not a forced state. The console agreeing costs nothing and disagreeing would have meant the same scenario looking forced on one surface and natural on the other. `happy` is also the one scenario with nothing to inject: A confirmed booking and a clean queue is the console's resting state, and announcing it would be the console narrating itself. | Yes. |
| 58 | 4b | Two scenarios carry a second, quieter band | `offline` and `loading` add `.injected--band` under the marker, saying the whole screen is stale or unconfirmed rather than one row of it. | Both are about the availability service rather than about a booking, so the honest statement is about everything below, not about an injected row. Separating it from the hatched marker keeps the marker's job single: What is forced, versus what that means for this data. | Yes. |
| 59 | 4b | `.injected` was added to `admin.css`, and `flask` to the console's icon sprite | Same hatching and same glyph as the customer build's marker, in the console's tokens. | The marker existed only in `index.html`. `flask` was the one glyph the marker needed that `admin.js` did not have, which rendered as a hole in the band rather than as an error, so it was caught by looking rather than by testing. Marker placement is inside `#main` as its first child, pulled out to the gutters: The desktop grid pins the app bar to row 1 and `#main` to row 2, so a third element between them would have landed on top of one of them. | Yes. |
| 60 | 4b | Two `set` keys that had nowhere to write were removed rather than hooked | `full` and `empty` each declared `op-registered` and `op-waitlist` overrides for `open-play.html`. No such hooks exist, so the applier silently skipped them. | A stub that does nothing is the thing this project's own rules forbid, and it does not stop being one because it lives in a data file rather than on a button. Hooking them was the alternative and was rejected: The Open Play page's first session already reads 8 of 8 and Full, so `full` had nothing to change, and writing "No window published" into a field labelled Registered is worse than the injected row that says it plainly. | Yes. |
| 61 | 4c | Admin is the console view of `opsmanager`, not a fifth surface | The Surface row gained a full-width "Admin / Club Manager, opens the console" control. It sets `S.surface = "opsmanager"`, writes the dials, and navigates to `admin/dashboard.html`. It carries no `aria-pressed`. | Rea Salvador is the Club Manager and `opsmanager` already exists. A fifth surface would mean inventing a fifth set of permissions for the same person and keeping the two in step forever. The control has no pressed state because the four above it are states this document can be in and this one is a door; a toggle that can never read as pressed is a control lying about itself. The harness copy says the same thing in a sentence, and `ALLOWED.surface` in the bridge still has four values, not five. | Yes. |
| 62 | 4 | Phase 4 added no drift to the default state | The mobile diff after Phase 4 is identical to the diff after Phase 3, apart from `app__demo` growing by the new surface control and scenario copy. | Worth recording because it is the design working: The console renders exactly as it did when no scenario is armed, and everything the hydration does is visible only while one is. A reviewer opening the console cold sees the reviewed sample data, not a hydrated approximation of it. | n/a |
| 63 | 5 | The oversized frame is scale-transformed, not left to overflow | `scale = min(1, room / (width + bezel))`, applied to the device with `transform-origin:top center`. A `.stage` wrapper carries the scaled height so a frame at 60% does not reserve its full height and leave a gap. | The prompt offered scaling or dropping the bezel; scaling is the answer to the fitting problem and it is the one that keeps the review honest. The iframe still LAYS OUT at the labelled width, verified: `contentWindow.innerWidth` reads 1280 and 1440 exactly, at every window size tested down to 700 pixels, so every media query fires as it would on a real machine and only the picture is smaller. Rejected alternative: Let the frame overflow and scroll, which keeps the type sharp and makes it impossible to see a 1440 layout AS a layout, which is the entire reason for looking at one. The cost is stated in the UI rather than hidden: Below 100% the note says the percentage and says to read layout here and not type. | Yes. |
| 64 | 5 | The bezel is also dropped above 1024, and that is a separate decision | `.device.is-desktop` drops the border to 1px and the radius to 4px. | Not part of the fitting problem and not an alternative to scaling. A 34 pixel phone bezel with a 34 pixel radius drawn around a 1440 pixel viewport is not a smaller phone, it is a category error, and it would have read as one at any scale. The note on screen says so at those widths. | Yes. |
| 65 | 5 | 768 x 1024 is labelled on the button, not only in prose | The button carries a `<small>` reading "below 1024, mobile chrome", and the note under the controls explains that the tab bar and hamburger are still the navigation at that width. | It is the one size that looks like a bug and is not. A reviewer opening a tablet width and finding a phone's bottom bar will report it unless the control that produced it says otherwise, and a sentence three paragraphs up is not where they will look. | Yes. |
| 66 | 5 | The default view is unchanged; the page around it is not | Still 390 x 844, Customer, unscaled, with the same content-box bezel. The page gained a Target row, a `<small>` on one size button, a note line, and a paragraph about shared origin. | The prompt asked for the default to look exactly as it does now, and the frame does. The chrome around it cannot, because a target switcher is a control that has to be visible to be used. Recorded so the difference is a decision rather than a surprise. | Yes. |
| 67 | 5 | The preview shares the harness dials with the frame, and says so | New paragraph: The frame and the page share an origin, so dials, the armed scenario, and the theme carry across both targets. | Not a change, an explanation of one. `localStorage` is per origin, so a scenario armed inside the Customer frame is already live when the Target switch loads the Admin one. That is useful and non-obvious, and without saying it a reviewer would assume the frame is sandboxed. | n/a |
| 68 | 6 | The order-scoped screens were added to the verification, not assumed | A second runner shoots `/hold/:id`, `/pay/:id`, `/status/:id`, `/save/:id`, `/booking/:code`, `/booking/:code/passes`, `/booking/:code/pass/1`, `/t/pass/:share`, and `/staff/booking/:id`, at two widths, on both trees. | None of them can be reached from a static index, because each needs a live order, so the main runner had never seen them, and they are the headline of this redesign. Result: 20 of 22 pixel-identical to the baseline. The two that differ are `/staff/booking/:id` at desktop, which now carries the rail because it is a `/staff/` path. All ten mobile shots are pixel-identical. | n/a |
| 69 | 6 | Two "failures" in the final sweep were the probe being wrong, and are recorded rather than quietly fixed | Assertion one checked `getComputedStyle(#tabhost).display === "none"` on a task route. Assertion two grepped for the string `admin/index.html` anywhere in a file. | On a task route `#tabhost` has no class, no children, and height 0. It is an empty `<nav>`, which is what the customer build has always rendered there, and the CSS that hides it keys off a class the router only adds on the staff shell. The correct test is `offsetHeight === 0`. The second matched the phrase inside a code comment in `index.html` explaining why the contact sheet was retired; the corrected test looks for `href` and `src` attributes only. Both are recorded because a green run that was green for the wrong reason is worth less than a red one. | n/a |
| 70 | 6 | `ADMIN-INTEGRATION.md` written | Architecture decision, the `STAGING` contract, the 1024 and two-chromes decision, the Admin-versus-`opsmanager` decision, the two-person cast, a click-through for every scenario on both surfaces, and how the verification works. | Deliverable 3. It explains the shape; this file explains the choices. | n/a |

## Sweep: Eleven UI findings (branch `ui-findings-sweep`)

A second task on the same repo. Numbering continues, because the log is
append-only and a restart would make two row 1s. The phase column carries a
`sweep` prefix, because this task has its own phases 0 to 4 and the bare numbers
above belong to the integration task.

| # | Phase | Decision | What changed | Why | Reversible? |
|---|---|---|---|---|---|
| 71 | sweep 0 | The signed-off nav work was committed to `main` before the branch was cut, so `main` is `07ec7cc`, not `e99da46` | The 226 uncommitted lines across `admin/admin.js`, `admin/admin.css`, `console-nav.js`, `console-nav.css`, and `index.html` were committed to `main` as one commit. `ui-findings-sweep` was then cut from it. | The prompt said to branch off `main` at `e99da46` and described the nav work as already signed off, but the two statements disagreed: The work was on disk and not in any commit. Leaving it dirty would have smeared five files of someone else's finished work through every commit in this task. Rejected alternative: Land it as commit 1 on the sweep branch, which keeps `main` at `e99da46` but mixes signed-off nav work into this task's diff. Confirmed with the user before acting. | Yes. `git reset --hard e99da46` on `main` puts it back and the work returns to the working tree. |
| 72 | sweep 0 | The prompt's "36 files differ by CRLF only" did not reproduce, and `.gitattributes` landed anyway | `git ls-files --eol` reports `i/lf` on all 50 text files and zero `i/crlf`. `git add --renormalize .` staged nothing. `.gitattributes` was committed alone as `1ba1c06` with `* text=auto` plus explicit `binary` for the image and font extensions. | The index was already clean because `core.autocrlf=true` is set globally on this machine, which is exactly the condition that hides the problem rather than fixing it. The file makes the guarantee a property of the repository instead of a property of one developer's config. The binary lines are not decoration: `text=auto` sniffs, and a JPEG whose first bytes read as text is a corrupted JPEG after EOL conversion. | Yes. Delete the file. |
| 73 | sweep 0 | The baseline is 112 shots, not 104, and the eight extra ones come from a second runner | `shoot.mjs` (reused from the integration task) captured 104: 26 routes and 27 admin pages at 390x844 and 1280x800. A new `states.mjs` captured 8 more: Staff Today in timeline and card view at both widths, staff Today at 1280 with the rail open and collapsed, and `bookings.html` and `staff.html` at 1280. | Four of the states this sweep has to protect are only reachable by pressing something, and `shoot.mjs` shoots first paint. They went in a separate runner rather than into `shoot.mjs` so that a broken click path cannot take 104 route shots down with it, and so the route sweep stays a route sweep. Both runners live outside the repo, as before. | n/a. Verification tooling, not product code. |
| 74 | sweep 0 | Baseline is clean on the branch point | 112 shots, 0 non-ok, 0 console errors, 0 failed requests. | Recorded so that any console error appearing later in this task is known to be new. | n/a |

| 75 | sweep 1a | The toast is one reused node with a visibility toggle, not a node per message | `toast(msg)` in `admin.js`, `.toast` in `admin.css`. Appended to `document.body` by `build()`. Both `alert()` call sites now route through it. | `#shell` is `overflow-x:clip`, which establishes a containing block, so a fixed toast parented there would be clipped at the moment it slides in from the bottom edge. One node also means a second message replaces the first, which is both the no-stacking rule and the reason the live region announces once per message rather than once per node. Rejected alternative: A container that appends and removes children, which stacks by default and needs a queue nobody asked to manage. | Yes. |
| 76 | sweep 1a | Under `prefers-reduced-motion` the dwell doubles rather than the timer being suppressed | 5s normally, 10s when reduced motion is set. Hover and focus-within stop the clock outright in both cases. | The prompt's sentence ("do not start it at all ... until the message has been on screen for the full duration") reads two ways, and this is the reading taken: The stated reason is the slow reader, and more time is what a slow reader needs. Suppressing auto-dismiss entirely was the other reading; it was rejected because a message that never leaves becomes furniture, and the dismiss button plus the hover and focus pause already cover the reader who needs unlimited time. Logged because it is an interpretation, not an instruction. | Yes. One constant. |
| 77 | sweep 1a | The toast returns focus, and only when it took it | On hide, if `document.activeElement` is inside the toast, focus goes back to whatever had it when the message appeared. If focus was never in the toast, nothing moves. | Not in the prompt, found in testing: Clicking Dismiss left focus on a button that was about to become `visibility:hidden`, which drops focus to `<body>` and loses a keyboard reader their place. The converse matters as much: A message timing out under someone who is typing must not move their cursor, so the restore is conditional rather than unconditional. | Yes. |
| 78 | sweep 1a | The toast is lifted by the dock's measured height, via a new `--toast-lift`, not via `--dock-h` | `admin.js` measures `.dock` once and writes `--toast-lift` on the root. `bookings.html` and `open-play.html` have docks; every other screen gets 0px. | Found in testing: At both widths the toast covered the dock, and the dock is the one action a screen exists to perform. `--dock-h` was the obvious variable to fill and was rejected: It is already inside two `main` padding calculations that currently resolve with it at 0px, so giving it a real value would move the foot of every docked screen, which is a layout change no finding in this sweep asked for. | Yes. Delete the token and the measure call. |
| 79 | sweep 1b | `[hidden]{ display:none !important }` was added to the console reset | One rule near the top of `admin.css`. | Found in testing, and it was the whole of the fix rather than a detail of it: `.row` sets its own `display`, an author `display` rule beats the browser's `[hidden]` rule, and so the first working version of the filter hid rows from the accessibility tree while leaving all seven of them painted under a heading that said zero. That is a worse lie than the pre-pressed chip this finding started as. `!important` rather than a specificity race with every component that will ever be filtered. | Yes. |
| 80 | sweep 1b | The rows carry `data-pay` with the word on their own pill, instead of the `data-paid` boolean the prompt asked for | `data-pay="paid|overdue|unpaid|refunded|none"`, and the Unpaid chip declares `data-match="pay=unpaid|overdue"` in its own markup. | The instruction was that the attribute and the pill must agree by construction. A boolean cannot: Row 3's pill says Overdue and row 7's says Refunded, and neither is honestly true or false. Overdue is unpaid and late, so the chip takes it; Refunded is money that moved and then moved back, so the chip does not. Writing the alternation on the chip puts the chip's definition of "unpaid" beside the chip, where a reader can disagree with it. `data-checkedin`, `data-status`, `data-court`, and `data-kind` landed as asked. | Yes. |
| 81 | sweep 1b | The segmented control was made to filter too, which the prompt did not ask for | The Bookings / Open Play / Blocked switch on `bookings.html` now selects rows by `data-kind`, and its three count pills are derived. | Constraint 5 says findings 2, 3, and 4 are one violation. Making the chips live while leaving the switch directly above them painting a pressed state over an unchanged list would have closed half of the finding on the page the finding is about, and left the louder of the two controls still lying. | Yes. |
| 82 | sweep 1b | A pressed mode renames the heading; a pressed filter is named in the announcement | The switch group carries `data-mode` and its options carry `data-noun`. Pressing Blocked gives "1 Blocked time", not "1 Booking, filtered by Blocked". | This is finding 7's distinction made audible before finding 7 makes it visible. A mode switch changes what kind of thing the list holds, so it changes the list's name; a filter narrows a set without renaming it. The two idioms now behave differently as well as, shortly, looking different. | Yes. |
| 83 | sweep 1b | The count announcement goes to a separate visually hidden live region, not to the visible toast | A second `role="status"` node, class `vh`, built beside the toast. | The prompt said to announce through the Phase 1a live region. The count is already on screen in the re-derived heading at the same instant, so routing it through the visible plate would state the same fact twice and put a plate over the list on every chip press. `index.html` already draws exactly this line between its screen-reader-only `announce()` and its visible UI. The module is shared; the plate is not. | Yes. |
| 84 | sweep 1b | Every count on both screens is derived, including the day rail's figure for the day being shown | `18 Bookings`, the three switch counts, `18 bk` on Saturday, and `11 more bookings later today` all came out. The first four are filled from the rows on load; the last was deleted. | The instruction was to leave no hardcoded number on either page. `11 more bookings later today` could not be made true without a list to count. The six other days in the rail keep their sample figures because they have no list behind them at all, and the sandbox note on the page now says so. | Yes. |
| 85 | sweep 1b | "All Courts" became an honest stub rather than a working chip or a dead toggle | It lost `aria-pressed`, gained `data-stub`, and now says it would open a court picker. | It is a picker trigger wearing a toggle's clothes: Pressed means what, exactly, when the label is "All Courts"? A real court filter needs a list of four courts and a sheet to hold them, which is a feature rather than a fix. Constraint 5 names this exact fallback: An honest stub beats a control that paints a pressed state over a list it does not touch. `data-court` still lands on every row, so a picker has its data waiting. | Yes. |
| 86 | sweep 1c | Branch B. The copy was made true and the grid was marked as sample; the grid was not derived | Empty state now reads "No customer bookings today. Open Play still runs, see the courts below." The Courts section carries the existing hatched `.injected` marker. The Held cell, the Blocked cell, and the venue-operation-window note are untouched. | (A) was refused for lack of a source, not for effort, and the check is on the record: `index.html` has no per-court maintenance state anywhere (the customer flow's `closedDay` is a day index, and `COURT_TIMES` is one venue-wide strip with no court dimension), and its only Open Play window is `OP_WINDOWS.w1` at `dayOffset:1`, which is tomorrow. `S.orders` does carry `assignedCourt` and `startsAt`, so bookings alone could have been placed, but the contradiction appears exactly when `S.orders` is empty. Deriving the grid would have meant inventing the two missing models and calling the result live, which is the failure this sweep exists to fix. The card view beneath is equally hardcoded, so there was no half-done derivation to finish either. | Yes. |
| 87 | sweep 2a | Most of the select finding was already fixed, and the log says so rather than claiming credit | `color-scheme` already tracked the active theme at `index.html:295-298`, with a comment naming "white select menus" as the reason. Nothing was needed there. | The prompt called this the one-line fix for the majority of the finding and asked for it first. It was done in an earlier pass. Recording it matters because the next reader would otherwise assume this sweep fixed the popup theme, and would look in the wrong place when it breaks. | n/a |
| 88 | sweep 2a | The `<option>` colours are set; the closed control keeps its ghost fill | `.field select option` gets an opaque `background-color` and `color`; `.field select` gets an explicit `color` only. | On Windows and Android the popup inherits the select's background, and this select's background is a ten percent ghost fill, so the list composited to grey type on a grey plate, which is what made the options read as disabled. Giving the options opaque colours removes the compositing. Making the closed control opaque as well was rejected: It would leave the one select on the event form looking unlike the eight inputs stacked around it, trading a fault nobody sees until they open the menu for one everybody sees on arrival. macOS ignores both and draws its own menu, which is the accepted cost of keeping a real `<select>`. | Yes. |
| 89 | sweep 2a | "Choose one" became a disabled placeholder option, via a new `placeholder` key on `fieldHtml` | `fieldHtml` emits `<option value="" disabled selected>` when `opts.placeholder` is set; the event-type field passes the prompt there instead of as the first list entry. | As a plain option it was a valid answer, and on a field nothing validates it would have been submitted often. Nothing reads `etype` in `sendevent`, so the change is safe today and correct the day something does. | Yes. |
| 90 | sweep 2b | The meters show a share of today's open tasks, and no capacity figure was invented | Both `.meter`s on `staff.html` gained the `.meter__top` line every other meter in the console already has. `admin.js` paints the number, the bar width, and the accessible name from one string, derived from `STAFF` plus `UNASSIGNED`. | The finding was neither an alt-text bug nor a missing pattern: `courts.html` and `open-play.html` both use `.meter__top`, and these two rows were the only meters without it. The old figures were 85% and 45% under the names "heavy" and "moderate", over two people carrying exactly two open tasks each. Nothing in the roster produces either number. A capacity figure would have preserved the old reading and would have been the one number in the console nobody could check, so the meter measures what the roster actually holds instead. Rejected alternative: `capacity: 3` on each roster entry, which is a guess wearing a variable name. | Yes. |
| 91 | sweep 2b | The crit fill came off Joy's meter | `meter__fill--crit` is no longer used on `staff.html`. | The pink was the meter trying to say "overdue" as well as "loaded", and her row already carries a 1 Overdue pill two lines above it. One fact stated twice in two idioms is how the bar and its accessible name got out of step in the first place. | Yes. |
| 92 | sweep 2c | `.seg` was already in `index.html`, so nothing was ported | The ghost button became a two-option `.seg` using `index.html`'s own rules at line 824. No CSS was copied from `admin.css`. | The prompt allowed porting the minimum needed and warned against forking a near-copy. Neither was necessary: This file has had the pattern all along, and five screens already use it. | n/a |
| 93 | sweep 2c | One focus key per segment, not the single `data-fk="tl"` the prompt named | `data-fk="tl-timeline"` and `data-fk="tl-cards"`. | Two buttons sharing one key means `querySelector` finds the first, so pressing Cards would return focus to Timeline. The My Bookings tabs directly above in the same file already do it per option (`data-fk="mbtab-upcoming"` and so on), so this follows the house pattern rather than inventing one. | Yes. |
| 94 | sweep 2c | `toggletimeline` sets the view instead of flipping it | The action reads `data-view` when present and falls back to the old flip when absent. | A segmented control has two options and pressing the one already selected must do nothing. A flip would have made it swap, so the control would have contradicted its own pressed state on every second press. The fallback keeps any caller with no view to name working. | Yes. |
| 95 | sweep 2d | The chip goes quiet and the segmented control keeps the inverse fill | `.filter[aria-pressed="true"]` now layers a ghost wash over its own ink plate with a full-strength 1.5px border and 700 weight. `.seg [aria-pressed="true"]` is unchanged. | The justifying sentence, from `bookings.html:54-58`: A segmented control switches what kind of thing you are looking at, and a chip narrows the set. A mode switch renames the list, so it takes the list's ink and wears it; a modifier does not change what the list is, so it does not. Going the other way round would put the loudest treatment in the console on the smallest claim. Selected is still carried by fill, border weight, and type weight, so none of it rests on colour. | Yes. |
| 96 | sweep 2d | A latent inversion bug in the same rule block was fixed, and the rest of its family was logged rather than swept up | `.filter[aria-pressed="true"]`, `.filter:active`, and `.filter[aria-pressed="true"]:active` now layer their wash as a `background-image` over `background-color:var(--card-surface)`. | Found in testing on the day theme, and it is a general trap in this stylesheet: The surface-inversion block near the foot re-bases `.filter` onto solid ink with a one-class selector, so any state rule up top carries more specificity, and writing `background:` takes the ink plate away with it. The wash then composites against the page, which on the day theme is cream under cream type. `.filter:active` had the same bug before this sweep. Several other `:active` rules still do; they are named in `UI-FINDINGS.md` rather than fixed here, because sweeping them is a change across a dozen components that no finding asked for. | Yes. |
| 97 | sweep 2e | The sandbox note lost its outline and gained a rule on the leading edge | `.sandbox` drops `border:1px dashed` and `border-radius:var(--r-card)`, and takes `border-inline-start:2px solid var(--border-default)` with matching padding. A `prefers-contrast:more` rule strengthens the rule and the type. | A rounded outline at the card radius, eight levels off the page on the night theme, reads as one more content card, which is the one thing the pattern's own comment says it must not be. A rule on the edge is an aside about the page rather than a box on it, and no card in this console has that shape. Rejected: A fill instead of an outline, which is a quieter card and still a card. Also rejected: Keeping the dash and taking it to `--hatch`, which would make this rhyme with `.injected`; hatching in this build means the harness has forced a state, and a note saying the whole screen is a prototype is a different claim. | Yes. |
| 98 | sweep 3a | CHANGED. `bk` is gone and the count is a pill, not a widened button | The six sample days render `<span class="countpill countpill--quiet tnum">14</span>` instead of `<i>14 bk</i>`. The closed day keeps `<i>Closed</i>` in the same slot. Every button gained an `aria-label`; the live day's is written by `admin.js` from the same count that fills the pill. | Neither of the two options in the prompt was taken, and the reason is the constraint it named: `Closed` has to live in that slot too. Widening to fit "18" plus an icon widens seven buttons to hold one word on one of them. Dropping the noun to leave a bare number puts "14" directly under "13", which reads as two dates. A count pill is a shape that already means "how many" everywhere else in this console, so it needs no noun and it leaves the closed day its whole word. The `aria-label` is not decoration: Three stacked fragments read aloud in order are not a sentence. | Yes. |
| 99 | sweep 3a | The pressed day flips its wash tokens locally | `.daybtn[aria-pressed="true"]` sets `--ghost-fill` and `--ghost-edge` to ink and gives the pill `color:var(--text-inverse)`. | The pressed day inverts to a cream plate, and `.countpill--quiet` draws its fill and hairline from those tokens, which up to that point point at cream. Left alone it is a cream pill on a cream plate with cream type. The tokens are flipped rather than the pill being given its own colours, which is the same move the surface inversion block at the foot of the file makes, one level further in. This is the third instance of the same trap in this sweep; see row 96. | Yes. |
| 100 | sweep 3b | NO CHANGE. The hatch is one meaning, deliberate, and already carries a second signal everywhere it appears | Nothing changed. | The finding asked whether the closed day and the blocked court ever share a view. They cannot: `.daybtn[data-state="closed"]` exists only in `admin/bookings.html` and `.tl-blocked` only in `index.html`. The wider question is the more interesting one, and the answer holds: The 45 degree hatch has about fifteen consumers in each file, and `admin.css:1172` states the single rule behind all of them, that ink is for what is live and actionable while anything finished, blocked, or absent drops back to hatched paper. Instances do share a view (`bookings.html` shows a closed day, an Open Play bar, a maintenance row, and a cancelled row at once) but they are the same claim about four different objects, and the object carrying the texture is what distinguishes them. Both cases the finding named also carry two further signals: A dashed border, and the word itself, "Closed" and "Blocked". A second signal is already there, so none was added. | n/a |
| 101 | sweep 3c | NO CHANGE. The caps stay, and they were already consistent | Nothing changed. | Consistency needed no work: `text-transform:uppercase` is one rule in `admin.css`, so all twenty seven pages have always agreed. The decision is therefore only whether to keep it, and it is kept. The app bar is chrome and the H1 is content, and this console's entire visual argument is that its chrome wears the inverse surface to say it is not the customer app; caps at 17px in a 56px bar belongs to that register. There is also no duplication to remove: The bar says "STAFF" and the H1 says "2 Staff Members", which are a label and a count rather than the same words twice. Dropping the caps would be a taste change with no defect behind it, and would spend the chrome-versus-content distinction to buy nothing. The longest title in the folder, "Staff Management" at sixteen characters, fits the centre slot at 390px with the existing ellipsis untouched. | n/a |
| 102 | sweep 4 | The `prefers-contrast:more` block was moved below both idioms, because it had been silently dead for one of them | The block moved from above the SEGMENTED section to after `.seg button:active`. | Caught by asserting on computed values in the final sweep rather than by reading the stylesheet. A media query adds no specificity, so `.seg [aria-pressed="true"]` written inside the query but above the base rule lost to it at equal weight, and the segmented control kept its 1.5px border with the reader asking for more contrast. The chip half worked only because it happened to sit below its own base rule. | Yes. |
| 103 | sweep 4 | Two customer routes flap by 22 pixels between captures of identical code, and this is recorded as a harness limit rather than chased | No change. `app__find` at 1280 and `app__openplay_w1_players` at 390 were shot three times on unchanged code: The first run put both above baseline, the second put both exactly on baseline, the third moved one back up. All 110 other files were pixel-identical across runs. | `shoot.mjs` predicts this by name and by pixel count in its own header: The peso sign is U+20B1, which sits in latin-ext, so a page with a price on it can begin a second font fetch after `document.fonts.ready` has already resolved, and shooting there catches a reflow worth 22 pixels. The explicit `FontFace.load()` pass reduces it and has not removed it. Chasing it further is work on the verification tool, not on the product, and the product evidence is that six consecutive measurements of `/find` gave the baseline height five times. | n/a |
| 104 | sweep 4 | `UI-FINDINGS.md` written, and it numbers the findings 1 to 11 in the order the brief raised them | New file. | Deliverable 3. The numbering agrees with four of the five findings the brief referred to by number in its own prose (2, 3, 5, and 7); the brief's reference to the sandbox note as "finding 11" is finding 8 in this ordering. The discrepancy is stated in the file rather than silently reconciled, so a reader holding both documents knows which way the numbers run. | n/a |
| 105 | sweep 4 | Nothing in the existing mobile CSS or the harness was changed except where a finding required it, and the two exceptions are named | `[hidden]{ display:none !important }` (row 79) and `.filter:active` (row 96) are the only rules touched that no finding named directly. | Both were required to make a finding's fix correct rather than merely present: Without the first, filtering hid rows from the accessibility tree while leaving them painted; without the second, the chip bar would have had one correct press feedback and one that painted cream on cream. No additive path existed for either, because both are corrections to how existing rules resolve rather than additions beside them. Everything else in the diff maps to a finding, and the Phase 4 height-change audit is in `UI-FINDINGS.md`. | Yes. |
| 106 | theme 0 | Branched from `ui-findings-sweep` at `7d96d82`, not at the `cdcd765` the brief named | Branch `theme-system` cut from the tip of `ui-findings-sweep` rather than three commits back. | The brief's stated reason for not starting from `main` was that the eleven findings are not merged there. `cdcd765` was the tip when the brief was written; the two commits added since are `92ab763`, which gitignores the task briefs and `.baseline/`, and `7d96d82`, which adds nineteen carousel images. Starting at `cdcd765` would satisfy the brief's reason and also un-ignore `.baseline/`, which this phase doubles to 208 PNGs, and drop 4MB of committed images. The tip satisfies the reason with neither cost. Rejected alternative: Branch at `cdcd765` and re-apply the two commits, which is the same tree by a longer route. | Yes. `git rebase --onto cdcd765` and re-add the two. |
| 107 | theme 0 | Baselines recaptured in both themes, and the band names carry the theme | `.baseline/` went from `{mobile,desktop}/` (112 shots, one theme per document, whichever that document defaults to) to `{mobile,desktop}-{light,dark}/` (208 shots). | A task that changes one document's light theme and must leave both dark themes untouched cannot be verified from a one-theme tree: The half that must not move was never photographed. The theme is pinned by seeding STAGING's own storage key before boot AND by setting the context colorScheme, so the `data-theme` copy and the `prefers-color-scheme` copy of every token block resolve together. The runner asserts `data-theme` after load, so a document that ignored the seed fails loudly instead of quietly producing a second copy of the other theme. | Yes. Delete and re-run with one theme. |
| 108 | theme 0 | The shot runner now waits for the document height to stop moving | `settle()` in the runner polls `scrollHeight` until it repeats three times, on top of the existing font and image waits. | Row 103 recorded two customer routes flapping by 22px between captures of identical code and accepted it as a harness limit. Re-measuring found four, always by exactly one line of text, which is a late reflow and not a font race: The router's second pass moves the page after `document.fonts.ready` has resolved and after every image has decoded. Polling the height is the only wait that catches it. | Yes. It is scratchpad tooling and is not in the repo. |
| 109 | theme 0 | `THEME.md` written before any value was spent | New file at the repo root. | Deliverable 2, and the artefact the rest of the task is checked against. It is the thing that stops the next screen inventing a colour: A screen that needs one it cannot find is supposed to add a row here first. | Yes. |
| 110 | theme 1a | The console's light neutrals moved; the customer build's did not | `--surface` `#FAF9F2` to `#F8F7F2`, `--surface-raised` `#FFFEF8` to `#F2EEDD`, `--surface-sunken` `#EDEBDE` to `#E4E0CE`, `--text-secondary` `#57574E` to `#605B52`, in `admin/admin.css` only. | The brief's neutral table lists the console's current values, and constraint 3 forbids regressing the customer build, which is signed off as cream cards on cream paper. Tokens carry their own value per document by design, so re-tuning one side is not a divergence, it is what the two-value system is for. Rejected alternative: Warm `--text-secondary` in both, which would repaint every subtitle in a signed-off build to buy consistency in a number nobody compares across documents. | Yes. |
| 111 | theme 1a | The review's `#C9C2AE` went to `--hairline`, and `--border-default` was tuned separately to `#8E8878` | Two tokens moved instead of one. `--hairline` `#E2DFCE` to `#C9C2AE`; `--border-default` `#A8A695` to `#8E8878`. | The two outlines do two jobs and one value cannot do both. A card edge is a rule and may be soft: `#C9C2AE` is right there, and it is what stops a cream card melting into an off-white page. A control edge owes 3:1 under WCAG 1.4.11, and `#C9C2AE` manages 1.53:1 on the new cream card, which would have made a day button's boundary unfindable. `#8E8878` is warmer than the value it replaces and reaches 3.04:1 on the card and 3.29:1 on the page, where `#A8A695` reached 2.33:1 and had been failing since before this task. Rejected alternative: Take `#C9C2AE` as given and let `prefers-contrast:more` carry the controls, which fixes the boundary only for readers who have already asked for it. | Yes. |
| 112 | theme 1b | `--critical` light was re-tuned in BOTH documents, including the customer build | `#B3261E` to `#C0392B` in `admin/admin.css` and in `index.html`. Dark untouched in both. | The brief scoped the change to the light theme and did not name a document. A semantic colour that means one thing has to look like one thing across both halves of one product, and the customer build carries the same token on its form validation. This is a change to the customer build and no additive path existed: The token is the value. Contrast is unaffected in practice: 4.6:1 on the customer cream, against `#B3261E`'s 5.6:1, both over AA. | Yes. |
| 113 | theme 1b | `--critical` moved only part of the way to the dark theme's coral, and the stopping point is a contrast floor | `#C0392B`, not `#F2857D`. | The finding is that the light red is too aggressive and the dark coral is the target. The coral is 1.6:1 on cream and cannot be type. `#C0392B` is as far as 4.5:1 allows: 4.67:1 on the new cream card, 5.07:1 on the page. Rejected alternative: Use the coral and stop using `--critical` as a type colour, which would mean redrawing eleven `.st--crit` pills, four plates, and two buttons as fills, and is a component change wearing a colour change's clothes. | Yes, once the pills stop being type. |
| 114 | theme 1b | `--caution` is an ochre, `#856110`, and not the terracotta the review asked for | The new family's hue. | The terracotta was tried first. At the luminance 4.5:1 on cream demands, a muted terracotta lands within about ten degrees of hue of `--critical`: Two reds of the same weight, which is a second red and not a second family, and it would have left Shift Unfilled exactly as loud as the thing it needs to be quieter than. The ochre costs a third hue in a build whose stated rule is two, and buys a distinction a reader can make before reading the word, which is the only reason to add a family at all. Rejected alternative: `#C96F5A` as suggested, which is 3.06:1 on the cream card and fails AA for the eleven pixel pills it would be used on. | Yes. |
| 115 | theme 1b | Every existing `--critical` usage was audited against the sentence, and six sites moved | `.plate--crit` to `.plate--caution` on `staff.html` (Shift Unfilled). `.sumcard--alert` to `.sumcard--caution` on `staff.html` (Pending Tasks) and `dashboard.html` (Pending Payments). `.st--crit` to `.st--caution` on `messages.html`, twice (Unanswered). `.meter__fill--crit` to `.meter__fill--caution` on `staff-details.html` (Workload heavy). The bell `.badge` and the solid `.countpill` to `--caution` in `admin.css`, and the collapsed rail dot in `console-nav.css`. | The sentence is: `--critical` means something is wrong or lost, `--caution` means something needs attention. Shift Unfilled is a gap in cover and nothing has been lost. The two tiles both count a backlog and name their one real problem in the note underneath, so painting the backlog in the colour of the problem is what made the problem unfindable. Unanswered is a queue. A heavy workload is not a failed one. An unread badge that is permanently red teaches a manager to stop seeing red. Everything that stayed critical stayed by the same test: Overdue, Failed, Unpaid, Collect, Conflict, Bookings Affected, and the three destructive buttons. | Yes, per site. |
| 116 | theme 1b | The solid `.countpill` defaults to `--caution`, with `.countpill--critical` as the named exception | Base class flipped; two sites take the modifier. | Counted across the console the solid pill is nearly always a queue: Six still to check in, five pending tasks, four unread, three maintenance jobs open, two unanswered messages. Two are genuinely counting loss and now say so: A player's payment issues on `players.html`, and the overdue share of a person's tasks on `staff-details.html`. The default follows the common case and the exception is explicit, rather than every queue in the building shouting. Rejected alternative: Leave the base red and add a caution modifier to nine sites, which puts the burden of remembering on every future page. | Yes. |
| 117 | theme 1c | The capacity scale exists, and Full stopped being drawn as void | `.chip--almost` and `.chip--atcapacity` in `index.html`; `.st--caution` and `.st--atcapacity` in `admin.css`. `CHIPS.almost` and `CHIPS.full` repointed. | Both documents drew Full with the treatment reserved for things that are GONE: Hatched and dashed, the same as cancelled, blocked, hold expired, and court out of service. A session that sold every seat has not gone anywhere, and drawing it that way left Full and Closed reading as one state on the one screen whose job is telling them apart. Full takes the ink plate, which is already how this build says a thing is settled and which costs no colour. Almost full takes `--caution` and is the only new colour in the scale. `.chip--full` keeps its meaning and its four other users. Rejected alternative: Give capacity its own third family, which the brief allowed only with a reason, and there is none: The scale is two neutrals and one caution. | Yes. |
| 118 | theme 1b | The status legend on `help.html` grew from six treatments to eight, and its opening sentence was rewritten | Two definition rows added (Ochre, Filled); the sentence about "anyone who cannot separate the red from the ink" now names both hues and states what each means. | A legend that documents six treatments on a build that draws eight is worse than no legend. This page is the console's own reference for a staff member who has to look a status up, so it is the one screen where the caution and critical sentence has to appear in the product and not only in `THEME.md`. | Yes. |
| 119 | theme 2 | THE REVERSAL. `--card-surface` in the light theme is cream, and the console's ink cards are gone | `--card-surface` light `#171717` to `#F2EEDD`. The SURFACE INVERSION section is renamed THE CARD PLANE and its comment now quotes what it replaced. | The comment being overturned read: "The page is the lightest thing in the build now, and the components sit on it as solid ink. That is the inversion: The customer app painted sheer ink washes on cream paper, and the console paints cream type on ink cards over a near white page. The two colours are unchanged, only which one carries the surface and which one carries the type." It is overturned because the ink slabs read as a different product rather than a denser one. Put the halves side by side and the customer app is cream paper and the console is a stack of black bricks. Density is type size, row height, and how much fits on a screen, all of which this console already does and keeps doing; it was carrying the density argument on a colour. After this the console is ink chrome around cream content, which is the shape the customer build already has. `--chrome-surface` is untouched and stays ink in both themes. | Yes. One token. |
| 120 | theme 2 | The old card block was not scoped to the dark theme, it was reduced, because thirteen of its fifteen declarations were dead | The 22 selector block went from `background` + `color` + 15 local token declarations to `background` + `--surface`. Four declarations (`--ghost-fill`, `--hairline`, `--hatch`, `--hatch-strong`) moved into a dark-only tail, along with the `.row--muted` / `.empty` paper reset. | The brief asked for the whole block to move behind the dark conditions. Checked against the dark root, thirteen of the fifteen tokens it set were the dark theme's own values written out a second time: `--text-primary`, `--text-secondary`, `--border-strong`, `--border-default`, `--surface-sunken`, `--surface-inverse`, `--text-inverse`, `--focus-ring`, `--focus-halo`, `--ghost-edge`, and all five `--critical-*`. `--surface-raised:#262626` was dead outright: All five consumers of that token are either not inside a card or are themselves in the card list and painted over. Carrying thirteen redundant declarations across the boundary would have doubled a 22 selector list to hold them. Rejected also: Keeping the block unconditional and adding a light block that undoes it, which is two rules for one fact. | Yes. |
| 121 | theme 2 | The dark tail is wrapped in `:where()` on BOTH halves, and the first attempt at it was wrong in a way worth recording | `:where(:root[data-theme="dark"]) :where(.card, .row, ...)`, weight 0,0,0. | Scoped the obvious way, `:root[data-theme="dark"] .card` weighs 0,2,0 and outranks `.daybtn[aria-pressed="true"]` and `.seg [aria-pressed="true"]` further up the file, which repaints every pressed control with the card fill. The first fix used `:is()` on the selector list, which is worse in a subtler way: `:is()` takes the specificity of its HEAVIEST argument, and `.datenav .iconbtn` in that list is two classes deep, so the card list weighed 0,2,0 while the paper island next to it weighed 0,1,0 and lost. The symptom was a 2000 pixel change in the dark theme on a maintenance pill nobody had touched, and it was found by diffing dark against dark rather than by reading the selector. Weighing nothing is safe here only because nothing competes for these four properties. | Yes. |
| 122 | theme 2 | Seventeen of the thirty four cream literals are gone, and the seventeen that remain are all palette declarations | Every `color-mix(in srgb, #F5F3DC N%, ...)` became `var(--text-primary)`; every `#F2857D N%` became `var(--critical)`; every `rgba(246,244,221,.N)` border became `rgba(var(--ghost-edge),.N)`. | Each substitution is exactly neutral in the dark theme, because `--text-primary`, `--critical`, and `--ghost-edge` all resolve to the literal that was there. That is the proof the re-basing did not move the dark theme: Not an argument, an identity. In the light theme they now resolve to ink, so a hover state means "further from the card" in both directions instead of "lighter" in one. What is left is fifteen token declarations, which is where a palette is allowed to state its values, plus the two chrome pins. | Yes. |
| 123 | theme 2 | The card outline needed no rule | Nothing added except the `prefers-contrast:more` reading. | Every card already draws `border:1px solid rgba(var(--ghost-edge),.22)`. It was invisible only because the old block set `--ghost-edge` to cream ON the card, so each card outlined itself in its own fill. With the override gone it resolves to ink at 22% over cream, within a shade of the `#C9C2AE` the review asked for. Rejected: Restating it as `var(--hairline)`, which reads as more deliberate and is worse. It is a second source of truth for a value the wash system already computes, and in the dark theme it resolves DARKER than the edge those cards have today, so it moved a theme this task may not touch. Caught by the dark diff. | Yes. |
| 124 | theme 2 | The two semantic hues were re-solved a second time, downward, after the audit | `--critical` light `#C0392B` to `#AC3227`; `--caution` light `#856110` to `#7B5A12`. | Phase 1 tuned both against a plain cream card. On a cream card they are fine; on `.row--crit`, which washes 13% of the same hue under them, `#C0392B` measured 3.89:1 and on a doubly washed pill 3.42:1. Both values are now the lightest that clear 4.5:1 against the darkest ground their own family paints, solved rather than guessed. The honest consequence: `#B3261E` to `#AC3227` is a small step, and the review's "the red is too aggressive" is bounded by AA at 12px rather than granted. What made the staff screen quieter was the family split, not the hue. Rejected: Keeping `#C0392B` and accepting 3.89:1, which is a knowing AA failure on eleven pages. Rejected: Lowering the 13% wash, which cannot be done per theme without a new token and would move dark. | Yes. |
| 125 | theme 2 | A pill inside a row of its own family drops its own wash | `.row--crit .st--crit` and seven sibling selectors set `background:transparent`, including on `.row--muted` and `.empty`. | Two 10% coats of the same hue under 12px type of that hue cost more than a whole step of the palette: 3.42:1 against 4.60:1 for the single coat. The row already says which family this is. Without this the solver put `--critical` at `#A02318`, DARKER than the `#B3261E` the review asked to soften, which would have made the finding worse while claiming to fix it. | Yes. |
| 126 | theme 2 | The dark theme's paper island now resets `--card-surface` too, and this is a deliberate dark-theme change | `--card-surface:#FAF9F2` added to the `.row--muted` / `.empty` reset. Visible on `check-in` and `court-details`. | Removing `color:#F5F3DC` from the card block exposed a bug the pin had been hiding: A button inside a no-show row took the island's ink type while keeping the theme's dark card fill, measuring 1.09:1 on the Contact button. The island exists to be paper, so components in it paint as paper. This moves the dark theme and is the only change in this phase that does so on purpose. Rejected: Restoring the pinned cream type, which reintroduces a literal and keeps a dark button sitting in a light row, which was always incoherent and merely legible. | Yes. |
| 127 | theme 2 | `.st` pins its own colour, and `console-nav.css` stopped hardcoding white on the hue | `.st{ color:var(--text-primary) }`. `console-nav.css` `.sidenav__row .countpill` from `background:var(--critical); color:#FFFFFF` to the caution pair. | A status pill is a pill, not body text: Inside the legend's `<dt>` it inherited the secondary tone and measured 4.35:1. The rail's count pill was 2.49:1 in the dark theme on all twenty seven console pages, because the light theme's "type on a solid fill" value had been written by hand instead of taken from the token that flips. Both are pre-existing failures that this task's audit found rather than caused. | Yes. |
| 128 | theme 2 | The contrast audit is a script that walks every element on every page in both themes, not a spot check | New scratchpad tool. Composites translucent layers and flat gradient tints down to the nearest opaque ancestor. | The brief said the cream-card change moves nearly every pair in the console and that this is not a spot check. It also caught a bug in itself worth recording: Chrome serialises a resolved `color-mix()` as `color(srgb 0.94 0.93 0.86)` with components in 0..1, and reading those as bytes turned every mixed fill in the build into near black, which reported 43 failures where there were 18. A contrast tool that is wrong is worse than none. | Yes, it is scratchpad tooling. |
| 129 | invoicing | Invoicing is planned as a document, not a billing term, and row 48 stays open | No code change. New file `INVOICING-PLAN.md` describes an optional **Generate invoice** action on the player side (`/me/history`) and the staff side (`admin/booking-details.html`, `admin/payments.html`), rendering a print-ready page: Logo top left, a translucent watermark centered and stretched to the margin box, line items and totals from the existing order shape, club details from `club.js`. | Row 48 left open whether this club invoices, and the two halves of that question have very different costs. Producing a document that describes money which has already moved is a rendering job. Changing when an unpaid hold is released, or accepting that a booking can sit payable for three days, is a change to the product's payment terms and to the customer build's ten-minute hold. This plan takes the first and refuses the second, so nobody later reads a **Generate invoice** button as having settled the overdue-bookings question. Rejected alternative: Treat the console's multi-day overdue state as evidence the club already invoices and build the billing term to match. Nine open questions are recorded in the plan rather than guessed at, tax being the one that could turn the whole feature into a compliance question. | Yes. It is a plan file and no build file was touched. |
| 130 | theme 3 | The filter rail is 8px taller, and the thing it was clipping was the focus ring rather than the chip | `.filterbar` padding-block 2px to 6px. Nine rails, 48px to 56px. | A scroll container cannot have a visible overflow on one axis: `overflow-x:auto` forces `overflow-y` to auto, so anything a chip paints outside itself is clipped by the rail. A focused chip paints a 3px outline at 2px offset plus a 5px halo, and 2px of padding cut the top and bottom off both. Six is what the ring needs. Rejected: A negative block margin to claw the height back, which was implemented and then removed. Every one of the nine rails carries its own authored `margin-top` in the page, so the compensation only landed on the bottom half and the rails grew by four instead of zero; a fix that is neutral on some pages and not others is worse than one that is the same everywhere. | Yes. |
| 131 | theme 3 | Two of the brief's three chip findings were not reproducible, and the measurements are recorded rather than the fix | No change for either. | The brief expected a pressed chip to grow by 1.5px and the first chip to sit against the gutter's edge. Measured on all nine rails in both themes: A pressed chip is 44.00px and an unpressed chip is 44.00px, because `.filter` is border-box with a 44px minimum, so the row does not twitch. The first chip sits at exactly the gutter, which is where the cards start, so it is aligned rather than jammed; the rail's negative inline margin is what lets it bleed past the gutter while its padding puts the first chip back on the grid. | n/a |
| 132 | theme 3 | The pressed chip's 1.5px border renders as 1px at device pixel ratio 1, and this is recorded rather than changed | No change. Noted in the stylesheet above the rule. | Found while measuring the rail. Chrome floors sub-pixel border widths to device pixels, so on a 1x desktop monitor the pressed chip, the pressed segment, and every 1.5px status pill are drawing one pixel less separation than the design asks for. On a phone at 2x or 3x they land as drawn, and this build is drawn for a phone. Changing it is a decision about the whole 1.5px idiom across four components, not about this rail, so it is logged and left. | n/a |
| 133 | theme 3 | The active chip fill was checked against the Phase 2d decision and needed no change, and here are the numbers | No change. | The brief expected a `--surface-inverse` fill on the selected chip to have become the heaviest thing on the page. It is not `--surface-inverse`: The previous sweep already made it `--card-surface` under a 28% ghost tint, precisely so the chip and the segmented control would read as different weights. Measured as luminance distance from the card it sits on: Light, chip 0.408 and segment 0.844; dark, chip 0.090 and segment 0.873. The two idioms are still two idioms. Worth recording that the light chip is proportionally the heavier of the two, because a 28% tint costs far more luminance going ink-onto-cream than cream-onto-ink, and the percentage is shared between the themes. It reads correctly and text on it is 8.4:1, so it stands. | n/a |
| 134 | theme 3 | The console's Open Play screen now carries all four capacity steps, and Full stopped being drawn as a void twice | `admin/open-play.html`: Evening Open Play to `.st--caution` "Almost Full" with the half-square glyph and a caution meter fill; Beginner Clinic to `.st--atcapacity`; the full session's card from an inline dashed hatch to `.card--strong`. | The screen had Open, Open, and Full, with the middle session two seats from capacity and drawn identically to one with five seats free. Full was drawn as void in two places at once, the pill and the card, both using the dashed hatch this build reserves for things that are GONE. The comment that justified the hatch ("so a full session does not read as merely another open one with a bigger bar") is answered by weight instead of by void, and is rewritten in place. | Yes. |
| 135 | theme 3 | A half-square glyph was added to the console's sprite rather than the scale being drawn differently on each side | `admin/admin.js` ICONS gains `squarehalf`, the same drawing as the customer build's `i-square-half`. | The two documents keep two sprites on purpose and `console-nav.js` says why. That argument is about not reconciling forty icons, not about refusing to draw one. A capacity scale whose middle step changes shape between the two halves of the product is not one scale, and the fill is what carries the glyph at 13px and in greyscale, which is the signal that has to survive when the ochre does not. | Yes. |
| 136 | theme 4 | The utility strip is a shell component both documents render, and the two functions that used to build it are gone | `console-nav.js` gains a `UTIL` model and `utilbar()`; `console-nav.css` gains the strip's look and the base `display:none`. `index.html` loses `themeButton()`, `demoButton()`, and eleven rules, keeping only its own `padding-inline`. `admin.js` gains `buildUtilbar()`. | Same division of labour as the sidebar beside it: The model and the markup are shared, the icon function comes from the document because the two sprites are deliberately unreconciled, and the click is wired per document because Demo means a harness sheet on one side and a staging notice on the other. Proof it is the same component: The customer build's strip is pixel identical across the change on every route at both viewports and both themes. A list rather than two hard coded buttons, because the strip's reason to be shared is that a third build control has one place to be added. | Yes. |
| 137 | theme 4 | THE PHONE ANSWER. The strip stays desktop only, and the phone reaches both controls from the menu the shell already has | Below 1024 the strip is `display:none` in both documents. The console's drawer gains a "This build" group at its foot with a theme row and a staging row; the customer build already had the same pair at the foot of its account sheet. | The strip costs 36px of permanent chrome. On a 390x844 console screen the app bar and tab bar already take 118 of 844, and a third strip would put the frame past 18% of the viewport to carry two controls a reader touches at most once a session. Both are already reachable on a phone from a menu that exists, so the cost buys nothing. This is not a new answer: The customer build made exactly this call at exactly this width, and its own comment states the rule, which is that no width shows two copies of either control. The console adopting it is one product with one answer rather than two products with two. Rejected: Putting the strip on a phone, which is the reading the brief left open and which trades a permanent 4% of every screen for a shortcut. Rejected: Leaving the console with nothing on a phone, which the brief explicitly forbids and which would have been the third silent divergence between the halves. | Yes. |
| 138 | theme 4 | The console's `--header-h` became a calc at every width rather than only inside the desktop query | `--utilbar-h:0px` at `:root`, `36px` inside the 1024 block, and `--header-h:calc(var(--utilbar-h) + var(--appbar-h))` once. | Same shape the customer build already used, adopted rather than invented. A token that only appears halfway up the breakpoint scale is a token every rule below it has to remember not to read; declared at zero from the start, one expression is correct at every width and the desktop query only has to change the height. The console's desktop grid gained a third row: The strip is above the app bar in the content column rather than a full width band, so the rail still spans everything and still reads as the frame. The customer build makes the opposite choice for the same reason, since it has no rail beside its strip. | Yes. |
| 139 | theme 4 | The staging notice dialog is built in this commit rather than the next, because a control with no action is not a delivered control | New `<dialog id="staging">` in `admin.js`, styled in `admin.css`. | The strip's Demo button has to do something the moment it exists. What it opens is the dialog the next phase is about, so it is written here and the next phase adds what is actually its own: The once per session first run, the memory through `STAGING`, and the per page note's contrast. Neither commit leaves the build in a state where a visible control does nothing. | Yes. |
| 140 | theme 4 | `console-nav.css` said the two builds were colour inverses of each other, which stopped being true two commits ago | The file header's palette sentence rewritten, and its title now names both components. | The file names no colours and did not have to change when the console's light theme reversed, which is the argument the sentence exists to make. Leaving the old wording would have made the file's own best evidence read as a mistake. | Yes. |
| 141 | theme 4 | `/find` at 1280 in the dark theme still flaps by 23px between captures of identical code, with identical rendered text | No change. Re-measured four times: 837, 837, 837, 860, with `#app` holding exactly 227 characters every time. | Row 103 recorded this as a harness limit and row 108 added a height-stability wait that fixed three of the four routes. This one survives it: The text content is byte identical across runs, so it is a wrap race rather than a content difference, and it is the only file in the whole two-theme tree that still moves on its own. Recorded so a later reader does not read it as a regression from this task. | n/a |
| 142 | theme 5 | The staging notice is a shared component too, and both documents show it | `notice()` in `console-nav.js`; the dialog element, the opening and the closing in each shell. | The brief named it as the console's, but the thing being replaced was the paragraph at the top of the customer build's harness sheet, which was the only place either half said what it was and which nobody read unless they went looking for the dials. It is one statement about one prototype; two copies would be two places for it to go out of date. Same division as the strip: Text here, dialog and wiring there, because the two shells already have their own modal idioms and neither needed a third. Two strings are passed in rather than shared, and both are passed in because they would otherwise be lies: The flask opens the harness on one side and this notice on the other, so the strip's accessible name and the notice's closing sentence differ. | Yes. |
| 143 | theme 5 | "Once per session" is `sessionStorage`, reached through `STAGING`, and is deliberately not in the persisted record | `STAGING.noticeSeen()` and `STAGING.markNoticeSeen()`, with an in-closure flag beside them. `reset()` clears both. | The dials are a preference and survive a close, because a reader who chose the night theme this morning wants it this afternoon. "I have read the notice" is not a preference, it is a fact about this arrival, and persisted it would mean somebody who saw it once in March never sees it again on a build that changes underneath them. sessionStorage is the matching lifetime and its per-tab scope is right for the same reason. The in-closure flag is not a fallback: The customer build is one page with hash routes, so once per session there means once per load and not once per route, and the flag is what says so even where storage is refused. Verified across fourteen assertions: First load opens it, Got it and Escape and a backdrop click all close it, focus returns to the flask, two navigations do not reopen it, a fresh session does, one session spanning both halves shows it once, and Reset all state brings it back. | Yes. |
| 144 | theme 5 | The dismiss button is outside the scrolling body | `.sheet__foot` in the shared markup, `flex:none` in the shared stylesheet. | Found by measuring rather than by looking: At 390x844 the customer build's copy is taller than its 40rem cap and the body scrolls, which put the one control that closes a dialog covering the whole page below the fold. Escape and the corner cross and the backdrop all still work; none of them is something to make a reader guess at. | Yes. |
| 145 | theme 5 | The notice's internal padding and heading size are set by the shared stylesheet, not by each shell | `.staging .sheet__head` and `.staging .sheet__body` override both shells' sheet padding. | The panel is 30rem wide in both. Left to the shells it was not treated as such: The customer build gives a sheet a 48px inline gutter and a 28px heading because its sheets are full width on a phone and this one never is, and the same words came out four lines taller there. One dialog that reads as one dialog. | Yes. |
| 146 | theme 5 | `.sandbox` needed no change, and the reason is worth recording | No change. Measured in four combinations. | The brief expected the per page note to need work "where a dashed hairline behaves differently than it did on ink". The dash is already gone: The last sweep replaced the outline with a 2px rule on the leading edge. What is left is the rule's own contrast, and it improved without being touched, because it draws in `--border-default` and phase 1a moved that token from `#A8A695` to `#8E8878` for unrelated reasons. Measured against the page: light 3.29:1 and 6.28:1 for the text, dark 3.53:1 and 7.38:1, and at `prefers-contrast:more` 16.71:1 and 16.00:1. The note stays, exactly as the brief asked, and its own comment's argument survives the dialog. | n/a |
| 147 | theme 5 | The harness's own opening paragraph stays | No change to `openDemo()`. | The brief says the dialog must not be the only place the staging state is stated. That paragraph explains which dials survive a refresh and which do not, which this dialog does not say and should not, and a reader who dismissed the notice an hour ago still needs the harness to say what it is when they open it. | Yes. |
| 148 | theme 5 | The shot runner marks the notice as already seen | `seed()` in the runner writes the sessionStorage key alongside the theme. | Every context the runner opens is a fresh session, so without it the regression tree would be 208 pictures of one dialog. The notice is captured deliberately instead, at four sizes and both themes. Proof the rest of the phase is inert: 206 of 208 files are pixel identical across it, and the two that are not are the two routes recorded in rows 103 and 141 as bistable on their own. | Yes. |
| 149 | theme 6 | Two-theme regression: 115 of 208 files moved, and every one is mapped to the phase that moved it | No change. A classifier maps each differing file to a phase and refuses to pass anything it cannot account for. | The brief's test is that every diff maps to a phase or it is a regression, which is a claim about all 115 rather than a spot check. Six console pages in the dark theme came back UNEXPLAINED on the first run and were chased to their pixels: The changed colour is 242,133,125 to 224,169,74, which is `--critical` to `--caution` at their dark values, so it is phase 1b's badge and count pill recast landing on every page that carries a bell. The classifier was corrected to say so rather than the finding waved through. Final state: Zero unexplained, three files accounted for by rows 103 and 141 as bistable on unchanged code. | n/a |
| 150 | theme 6 | The contrast audit is over every element on every page in both themes, and the console ends with zero failing pairs | No change. | 27 console pages plus 8 customer routes, walking every element that carries its own text, compositing translucent layers and flat gradient tints down to the nearest opaque ancestor. The console fails nothing in either theme. Everything still failing is in the customer build and is either an artefact of the method (chrome over the header's photographic fade, which the walker cannot sample) or predates this branch. | n/a |
| 151 | theme 6 | THE THREE WORST RATIOS ON THE SHIPPED PALETTE, which all pass | No change. | Light console: 4.58:1 `.plate__head` `#AC3227` on `#E9D6C5` at 12px 700; 4.65:1 `.btn--danger` `#AC3227` on `#EAD7C7` at 15px 700; 4.76:1 `.plate__meta` `#605B52` on `#E9D6C5` at 14px 400. All owe 4.5:1 and clear it, the tightest by 0.08, which is exactly the constraint row 124's solver was aimed at: The red is as light as the 13% wash under it allows. Dark console: 5.28:1, 5.33:1, 5.43:1. Nothing in either theme is close enough to move on rounding. | n/a |
| 152 | theme 6 | WHAT GOT WORSE, measured against a worktree of the commit this branch was cut from rather than asserted | No change. A second copy of `7d96d82` was served on another port and the same audit run against both, matching pairs by class and type size. | 82 text and ground pairs exist on both sides in the light console and 75 of them moved, which is what reversing a theme does. NONE crossed below its threshold. The largest falls are all from very high to comfortable: 7.83 to 5.09 on the note under a day button, 17.74 to 15.41 on the sidebar label and on inline bold, 7.38 to 5.79 on every secondary line in a row. That is the shape of the inversion: Cream type on ink was 17:1 and ink type on cream is 15:1, and both are far past anything that matters. In the dark console only 3 pairs of 82 moved at all, and one of them crossed UPWARD: The rail's count pill from 2.49:1 to 8.49:1. | n/a |
| 153 | theme 6 | Two contrast failures were found in the customer build, are older than this branch, and are NOT fixed here | No change. Both measured identically on `7d96d82` and on HEAD. | `.hr__t` on the muted hours of `/court` is 3.07:1, `#8B8B7F` on `#F5F3DC` at 17px 700, which owes 4.5:1 because 17px is under the 18.66px that makes bold type large. `.footnote` at the foot of three routes is 3.96:1, `#6E6B67` on a genuinely black `.pagefoot`, at 12px 300, in both themes. Neither is touched by any finding in this brief and both are in a build constraint 3 protects, so fixing them on my own initiative is the scope creep the constraint exists to prevent. The fixes are one token each: `--ink-400` to `--text-secondary` on `.hr__t`, and a lighter value on `.footnote`. Recorded here so they are picked up deliberately rather than found again. | n/a |
| 154 | theme 6 | The remaining sweep items, each asserted on the live build rather than on the stylesheet | No change. | `prefers-contrast:more`, both themes: The card edge goes from a 22% wash to solid `--border-default`, the pressed chip from 1px to 2px, the sandbox note from 2px `--border-default` to 3px `--border-strong`, the strip's demo button from 30% at 0.72 opacity to solid at 1. `prefers-reduced-motion:reduce`, both themes: Nothing on a changed surface runs longer than 1ms. Colour is not the only signal: Every capacity step carries a word, a glyph, and a border or hatch, and every caution and critical component carries a word, a glyph, and a bar or a border. Keyboard: The strip's two buttons and the filter chips all take focus with a 3px outline at 2px offset plus a halo and none is clipped by the rail; the dialog traps Tab, closes on Escape, and returns focus to the flask. Console clean: 27 pages and 8 routes in both themes, no errors, no failed requests, no 4xx. `v2/assets/logo.png` returns 200 from `/admin/` and the wordmark carries `invert(1)` in both themes on both sides. | n/a |
| 155 | theme 6 | `THEME.md` is checked against the build mechanically, not by reading | No change. 116 token values read out of the running documents and compared to the tables. | A palette document that has drifted from the palette is worse than none, and this one was edited three times while values moved under it. 116 checked, 0 mismatches. The checker also had to learn that section 4 is console-only, which it says in its own heading; without that it reported the customer build as missing two tokens the document never claimed it had. | n/a |
| 156 | theme 6 | This task's rows were renumbered from 129 up, because an unrelated row took 129 while the branch was mid flight | Every `theme 3` to `theme 6` row shifted by one, 129 to 154 becoming 130 to 155. The two rows that cite row 140 by number now cite 141. | An `invoicing` row was added to the working copy between the phase 2 and phase 3 commits and swept into the phase 3 commit by a `git add -A`, which also gave it the number this task's next row was about to take. Two rows sharing a key makes one of them uncitable, and five rows in this task cross-reference each other by number. The unrelated row keeps 129 because it was there first. Also recorded: `INVOICING-PLAN.md` was swept into a commit the same way and removed from it; the file is untracked and on disk, and where it belongs is not this task's call. | Yes. |
| 157 | theme 6 | The house rule about capitalizing after a colon was applied to 58 lines this branch added, including comments | 55 lines by script plus three by hand, across `THEME.md`, `DECISIONS.md`, both stylesheets, three scripts and two console pages. | The folder's rule names Markdown, TXT, HTML, headings, table cells and body prose. It reaches code comments too, and the proof is the code that was already written to it: `admin.css` had "which is the point: The console's mobile layout" and "Above it, both go: The bar because" before this branch touched it. Writing the new comments the other way would have left one stylesheet following two conventions. The fixer skips CSS declarations, object literal keys, media query preludes, URLs, filenames, times and ratios, and anything inside backticks, and every proposed edit was read before it was applied. Cost: 32 pixels on one page, the status legend's opening sentence, which is the only one of the 58 that is rendered prose rather than a comment. | Yes. |
| 158 | theme 6 | The pressed day's count pill was invisible in the light theme, and this was the on-ink recheck missing one | `admin.css`: The pin that set `--ghost-fill` and `--ghost-edge` to ink on a pressed day is gone; the pill joins the pressed segment and the pressed filter in the rule that derives its fill and edge from `--text-inverse`. | Phase 2 was told to recheck every assumption that was riding on cards being ink, and this one was not on the list it checked. The pin existed because the pressed plate was cream in BOTH themes: The old card block flipped `--surface-inverse`, so a selected day was cream even in the light theme. With the cards cream the plate is ink in light, and the pin painted an ink pill with an ink edge on an ink plate. The edge measured 1.00:1, which is to say the pill was not drawn. It is now 11.59:1 in light and 12.55:1 in dark. Found by an independent review of the branch, not by the phase that owned it. Costs 245 pixels on one page in the light theme, and moves the dark pill's fill by about 2%, which is the price of three rules becoming one. | Yes. |
| 159 | theme 6 | The comment the brief named by line number was never rewritten, because a near-copy of it was rewritten instead | `admin.css:84-88`, above the light token block, now states what the console does and quotes what it used to claim. | The brief quoted `admin.css:85-89` and said to rewrite it rather than delete it. The same sentences appear twice in that file, once over the token block and once as the section header at its foot, and only the second was rewritten. The first was left asserting "the console paints cream type on ink cards over a near white page" directly above the tokens that make it false, which is the worst place in the file for it to be wrong. Found by an independent review; the phase that owned it reported it done. | Yes. |
| 160 | theme 6 | Four more comments were stale or orphaned and are corrected | The `--critical` note still gave `#C0392B`, which phase 2 moved to `#AC3227`. The `:where()` safety argument counted two declaration sites where there are three. The pressed filter chip's rationale still described the block it names as making things ink. `console-nav.css` had a reduced-motion comment stranded 120 lines from its rule by two blocks this branch inserted at the same anchor. `index.html`'s ATTENTION RED heading still said one family. | All five are the same failure: A comment written correctly and then invalidated by a later commit on the same branch, or by an insertion above it. A stylesheet where the prose disagrees with the rule under it is worse than one with no prose, because a reader trusts it. Found by an independent review of the finished branch, which is the argument for running one. | Yes. |
| 161 | theme 6 | The colon rule was applied to the whole of `DECISIONS.md`, not only to this task's rows | 44 words capitalized across the file, 16 of them in rows this task added and 28 in rows from the two tasks before it. Two more in `THEME.md`. | The rule ends "Apply the rule when writing new content and when editing existing content. Do not leave a file half-converted." Fixing only the new rows is exactly the half-converted state it names, in a file that reads as one document. The edits are one letter each and change no meaning; the fixer skips backticks, URLs, filenames, times, and ratios, and its output was read before it was applied. | Yes. |
| 162 | theme 6 | The two semantic families measure 1.02:1 against each other, so neither is allowed to depend on its hue | `.st--caution` gains a 5px leading edge, `.plate--caution` goes dashed against the red plate's solid, and every caution site in the console takes the flag glyph where critical keeps the warning triangle. The legend on `help.html` names both treatments. | `--critical` is `#AC3227` at luminance 0.112 and `--caution` is `#7B5A12` at 0.116. That is not a tuning error to fix: both owe 4.5:1 as small type on cream, which pins both near the same lightness, and ANY pair that clears AA on this palette will be close. Hue was therefore the only axis separating them, and hue is the one axis this build has always refused to depend on. Before this, Shift Unfilled and 1 Overdue on the same screen carried the same glyph, the same border, and the same lightness. Printed in black and white they were one thing. They are now two. Found by an independent review; my own audit measured every pair against its BACKGROUND and never against the other family, which is the hole. | Yes. |
| 163 | theme 6 | Control boundaries in the light console fell to between 1.8:1 and 2.6:1, and this is reported rather than fixed at rest | No change at rest. At `prefers-contrast:more` every control now takes `--border-strong`. | Measured, light console, before and after: the filter chip 2.39:1 to 1.83:1, the search field 2.55:1 to 1.92:1, the secondary button 3.81:1 to 2.62:1. The day button is 3.04:1 because it is the one control drawn with `--border-default`; every other control draws its edge from the `--ghost-edge` wash at 22 to 42 percent, which was already under the 3:1 WCAG 1.4.11 asks and is further under it on cream than it was on ink. THIS IS THE REAL COST OF THE INVERSION and my own "what got worse" pass missed it, because that pass compared text against its background and never compared a border against what it borders. Fixing it at rest means changing the ghost wash idiom across both documents and both themes, which no finding asked for and which constraint 4 and constraint 3 both push back on. What is fixed is the escape hatch: A reader who has asked for more contrast now gets 15:1 on every control rather than a card-only improvement. | The at-rest change is available: Point the control family at `--border-default`, and accept that it moves the dark theme. |
| 164 | theme 6 | The first version of that fix made the dark theme WORSE and was replaced | `prefers-contrast:more` went from `--border-default` to `--border-strong` on controls. | `--border-default` reads as the milder, more proportionate choice and is not. In the dark theme the cream wash at 42% is already lighter than `#6F6F63`, so pinning that token took the secondary button from 2.67:1 down to 2.33:1 while claiming to raise it. Caught by measuring the fix rather than by shipping it. `--border-strong` cannot do that in either direction: 15.41:1 light, 14.71:1 dark, on every control. | Yes. |
| 165 | theme 6 | Three more accessibility defects, all introduced by this branch, all fixed | The console's theme toggle threw keyboard focus to `<body>` on activation, because repainting the strip destroys the button that was focused. The strip's Demo button had an accessible name that did not contain its visible label. The customer build's notice handed focus back to whatever the router had focused, which is a heading nobody chose. | The first is the worst: A reader presses Enter on the toggle, the page's colours change, and they are nowhere. The strip is now repainted by action name, which survives the rebuild because it identifies the control rather than its markup. The second is WCAG 2.5.3 and not pedantry: The visible word is "Demo" and somebody using voice control says what they can see, so the name now starts with it. The third treats `tabindex="-1"` as "focused by script, not by a person" and falls through to the flask. | Yes. |
| 166 | theme 6 | One review finding did not reproduce | `.st--off` was reported as falling to 3.82:1 in the light console. Measured on every page that renders one: `closures.html` 6.09:1 to 5.09:1 and `help.html` 7.83:1 to 5.09:1. | The fall is real and is the same `--text-secondary` and `--surface-sunken` move that shows up across the log; the failing number is not. Recorded because a review finding that is wrong is worth writing down as wrong, so it is not rediscovered and half-believed later. | n/a |
| 167 | theme 6 | `THEME.md` named the wrong token for the mechanism that keeps `--card-surface` and `--surface-raised` apart | Section 4 and the token table rewritten. | It said a console card redeclares `--surface-raised` locally. It did, before this task: The card block carried `--surface-raised:#262626`, nothing read it, and row 120 deleted it as dead. The sentence outlived the code by three commits, and phase 6.7 requires this document to match what shipped. The card redeclares `--surface`, which is what the code comment always said. The scope that actually keeps the two names apart in shipped code is the dark theme's paper island, which overrides `--card-surface` to `#FAF9F2` and leaves `--surface-raised` at `#1F1F1F`; merge the names and the Contact button in a no-show row goes back to 1.09:1. So constraint 1 holds, but for a reason the document was not giving. | Yes. |
| 168 | theme 6 | `.st--off` lost its muted type, because muted type on a hatch fails AA on the new palette | `color:var(--text-secondary)` removed; the pill takes `--text-primary` from `.st`. | 3.85:1 in the light theme against the hatched ground, under the 4.5:1 a 12px bold pill owes, and 4.57:1 before the palette moved. Every other route was measured and none pays it: Reverting `--text-secondary` reaches 4.26:1 and reverting `--surface-sunken` as well reaches 4.24:1, because the hatch takes the last of the headroom either way. Dropping the colour reaches 10.23:1 and loses nothing, because the state was never being carried by it: The dashed edge, the sunken fill, and the hatch already say inert three times. Found by an independent review; my own audit composited flat fills and flat gradient tints and read a repeating hatch as if it were its pale half, which is the hole. | Yes. |
| 169 | theme 6 | The `prefers-contrast:more` card rule was silently downgrading `.plate--work` | `--border-default` to `--border-strong` on the card family, matching the control family. | The rule is a bare one-class selector list placed after every variant rule on the same elements, so at more contrast it overrides all of them. `.plate--work` draws a 3px double `--border-strong` rule; the block that exists to raise contrast was taking it down to `--border-default`. `--border-strong` cannot do that in either direction. `.pick span` was also missing from the control list and is added. | Yes. |
| 170 | theme 6 | The console's utility strip is sticky, because `--header-h` was otherwise reserving space for something that scrolls away | `.utilbar` gains `position:sticky; top:0` and the page's own background; `.appbar` sticks below it at `top:var(--utilbar-h)`. | `--header-h` is `calc(--utilbar-h + --appbar-h)` and its only reader is `scroll-padding-top`. With the strip not sticky, 36px of that reservation was a lie and an in-page anchor landed 36px short of where it claimed. Two choices: Stop counting the strip, or make the count true. Making it true also matches the customer build, whose strip is inside a sticky header group, and keeps the theme and demo controls reachable without scrolling to the top. | Yes. |
| 171 | theme 6 | The staging notice had nowhere to return focus to on a console detail screen | `stagingAnchor()` gains the app bar's back control, and the page title as a last resort. | A screen that declares `data-back` builds no drawer and shows a back arrow instead of a hamburger, so on a phone neither the strip nor the menu button is on the page and focus was landing on `<body>`. Reproduced on `tasks`, `payments`, `notifications`, and `player-details`; all four now return to the back arrow. | Yes. |
| 172 | theme 6 | `aria-pressed` is removed from the theme control in all three places it appears, reversing a decision from the previous sweep | The strip's toggle, the console drawer's row, and the customer build's account sheet row. | `aria-pressed` says a control is IN a state; every one of these three names what pressing it will DO. Together a screen reader announces "Switch to the day theme, pressed", and there is no reading of that which says which theme is on. One of the two had to go. The name stays because it is the half a reader can act on and because the control is phrased as an action in all three places. What the theme currently is stays discoverable the way it always was, by looking at the page the control changes. The customer build's row is included even though it predates this branch, because fixing two of the three is a product that contradicts itself in one place instead of three. | Yes. |
| 173 | theme 6 | The independent review returned 31 candidates, 12 survived refutation, and one of the twelve was still wrong | 8 fixed here and in the two commits before it; 2 refuted by measurement; 2 recorded as reported rather than fixed. | Worth recording what the review was worth. The two findings nobody else would have caught are the isoluminant families (row 162) and muted type on a hatch (row 168), both of which my own audit was structurally unable to see: It compared every pair against its background and never against the other family, and it read a repeating hatch as its pale half. Two findings did not survive: `.st--off` at 3.82:1 was attributed to the wrong cause, and `--card-surface` was called a duplicate name when it verifiably diverges in the dark theme's paper island. Two more are reported and not fixed: Control boundaries at rest (row 163) and the two pre-existing customer build failures (row 152). | n/a |

## Port: The console's theme onto the customer build (branch `theme-system`)

Numbering continues. The phase column carries a `port` prefix. The task was
stated as "apply this UI theme to the rest of the mobile staging, including the
visitor/customer/player side. Exceptions being the features for admin/OM/staff
remain for them only. Basically only theme changes." So: The console's look
travels; none of its functionality does.

| # | Phase | Decision | What changed | Why | Reversible? |
|---|---|---|---|---|---|
| 174 | port 0 | The depth of the port was asked rather than assumed, because three readings of it produced three different builds | Chosen: Surfaces and chrome, hero photograph kept. Rejected by the user: Surfaces only, which keeps the ghost navigation; and the full version, which drops the photographic hero. | "Apply this theme" is unambiguous about the palette and silent about the header, and the header was the one component the two halves genuinely disagreed on. Guessing it would have been a redesign presented as a theme change. | n/a |
| 175 | port 0 | The three uncommitted files were copied out before anything was edited | `index.html`, `admin/admin.css`, `console-nav.css` and a `git diff` patch, into the session scratchpad. | The branch had 349 uncommitted lines of signed-off theme work on it. A port this wide over an unsaved diff has no floor to fall back to. | n/a |
| 176 | port 1 | The customer build takes the console's neutral values outright rather than deriving its own | Nine light tokens moved; the dark theme already agreed and gained only the two pinned surfaces. | `THEME.md` argued for separate values on the grounds that a customer card sits on photography. One of twenty-six customer screens has a photograph on it. The other twenty-five were paying for the hero in sheer washes over a page with nothing behind them. | Yes, one block. |
| 177 | port 1 | `--border-default` is the one neutral solved rather than copied, and it landed on the console's value anyway | `#838172` to `#837E6F`. | The customer build draws controls on `--surface-sunken` (a closed day button, a dead pass box) and the console does not, so the customer value had to clear a ground the console's never did. Solved independently it came out at 3.83 / 3.54 / 3.11 against page, card and sunken; the console's `#837E6F` clears the same three. Two files now hold one value instead of two that agree by luck. | Yes. |
| 178 | port 2 | The ghost navigation is replaced by the console's ink plate, and the plate sits on `.appbar` rather than on `.headergroup` | `.headergroup` becomes the page; `.appbar` and `.tabbar` take `--chrome-surface` and pin six tokens each. | Painting the group would have made the desktop utility strip ink too. The console renders that strip on the page with the navigation ink under it, and splitting the paint the same way is what reproduces it. | Yes. |
| 179 | port 2 | Four conditional wordmark inversions become one unconditional rule | `:root[data-theme=dark]`, the `prefers-color-scheme` copy, `body.shell-staff`, and `:root.over-hero` all deleted. | All four said "invert" and only the condition varied. With the bar ink on every route, in every theme, on every surface, the condition is gone rather than made permanently true. | Yes. |
| 180 | port 3 | The hero keeps its glass, and it keeps it for free | No `.hero`-scoped rule was touched. | Every hero surface was already an override of the base component, so re-basing the base left the photograph's treatment intact without a single exception being written. | n/a |
| 181 | port 3 | The bottom tab bar was themed, not extended to the customer surfaces | `.tabbar` takes the ink plate; `has-tabbar` is still set only for the staff shell. | The visitor and player shells navigate from the header and never had a bottom bar. Giving them one is a navigation change, which the task explicitly excluded. | n/a |
| 182 | port 4 | The port's first version regressed the dark theme, and a differential audit caught it rather than a screenshot | The card family had been given `--hairline`; it went back to `rgba(var(--ghost-edge),.22)`. | The two are a shade apart in light, which is what made the swap look free. In dark the wash is cream and `--hairline` is darker than the card, so every card and row fell from 1.98:1 to 1.23:1 on the theme where the fill does least. The console never made that swap. | Fixed. |
| 183 | port 4 | Findings were measured against the pre-port file, not against the guidelines | The same sweep, 18 routes by 2 themes, run on a copy of the original and diffed. | A build this old has failures that predate the task. Without a baseline every one of them reads as something this branch broke, and the two that mattered would have been buried under nine that did not. | n/a |
| 184 | port 4 | The three header controls stop ranking themselves by edge alpha | `.42 / .35 / .28` all become `.42`. | On the new plate that ranking read 3.82 / 3.02 / 2.47:1, so it was spending the one axis that owes 3:1 to say something the fill and the label already say. Same argument the `.btn` weights took two commits earlier. | Yes. |
| 185 | port 4 | `.chip--quiet` painted the card's colour on the card's colour, which is a consequence of the local `--surface` redeclaration rather than of any value | `var(--surface)` to `var(--surface-sunken)`. | A card redeclares `--surface` so its children resolve against it, so a child whose own fill is `var(--surface)` has no fill. `--surface-sunken` is redeclared by nothing and steps below either ground. | Yes. |
| 186 | port 4 | Two findings were left as reported rather than fixed, and one number in the sweep is wrong on purpose | The black footer's `#6E6B67` at 3.96:1, untouched by this branch; and `.chip--quiet`'s dark edge, which the metric reads as 1.35 to 1.23 while the pill actually gained a fill step it never had. | The footer is out of scope and pre-existing. The chip measurement compares a border to a ground that moved; comparing the pill to what it sits in, it is more findable than before, which a screenshot confirms. | n/a |
| 187 | port 5 | `.hr[data-off]` was fixed although it was pre-existing, because it is a colour and this is a colour task | `var(--ink-400)` to `var(--text-secondary)`: 2.68:1 before the port, 2.96:1 after it, 5.77:1 now. | A legacy literal from before the token layer, on the row whose entire job is saying which hours are gone. In scope by the same rule that put every other value in this task in scope. | Yes. |
| 188 | port 5 | `index.html` gains the `prefers-contrast:more` block it had nothing to raise before | Card family and control family to `--border-strong`, mirroring `admin.css`. | Its cards were washes and its buttons ranked by alpha, so there was nothing this shape to strengthen. Both are gone, so the rule that goes with them came across with the rest. | Yes. |
| 189 | port 5 | Two claims in `THEME.md` were wrong and are corrected rather than left | The console `--border-default` row still read `#8E8878` / `#6F6F63` against a file holding `#837E6F` / `#7A7A6D`; and the card family was said to take `--border-default` at `prefers-contrast:more` when row 169 had already moved it to `--border-strong`. | Both were found by reading the file to copy it. A token table that disagrees with the stylesheet is worse than none, because it is the thing the next reader trusts. | n/a |
